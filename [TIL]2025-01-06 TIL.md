# [2025.01.06]이산수학과 논리설계-TIL

# 이산수학(Discrete Mathmatics)

> 불연속적인 객체를 연구하는 수학
> 

## 1. 집합(Sets)

> 순서가 지정되지 않은 원소 모임
> 

### 집합 표기법

- 원소 나열법
- 조건 제시법

### 집합 종류

- 전체집합
- 공집합
- 부분집합
- 멱집합
- 합집합
- 교집합
- 여집합
- 차집합

## 2. 순열&조합

### 순열

> 서로 다른 원소들을 순서를 고려하여 일렬로 배열하는 것
> 

$$
P(n,r)= _nP_r = \frac{n!}{(n-r)!}
$$

### 조합

> 서로 다른 원소들을 순서를 고려하지 않고 일렬로 배열하는 것
> 

$$
C(n,r)= _nC_r = \frac{n!}{(n-r)! \cdot r!}
$$

## 3. 함수

```
f: X -> Y
```

- f를 X에서 Y로의 함수(function)라고 하며, 위 같이 표시한다.
- X는 f의 정의역(domain), Y를 f의 공역(codomain)이라고 한다.
- 함수를 간혹 사상(mapping) 또는 변환(transformation)이라고도 부름

### 일대일 함수(Injective Function)

> 함수의 각 입력 값에 대해 유일한 출력값에 대응되는 함수
> 
- 함수 f: A → B가 일대일 함수일 때, f(x1) = f(x2) ⇒ x1 = x2

### 전사

- 공역의 모든 원소가 적어도 하나의 정의역 원소에 대응해야 함

### 단사

- 일대일 함수와 동일한 의미
- 정의역의 서로 다른 원소들이 공역의 서로 다른 원소에 대응되는 함수

### 전단사

- 각 정의역 원소가 하나의 공역 원소에 대응되고, 공역의 모든 원소가 정의역 원소에 대응

## 4. 관계

### 이진관계

두 집합 A와 B의 원소들 사이의 관계를 나타내며, 순서쌍의 집합으로 표현됨

### 집합 위로의 이진관계

하나의 집합에서 정의되는 이진관계로, 같은 집합의 원소들 간의 관계를 나타냄

### 반사적인 관계

집합의 모든 원소가 자기 자신과 관계를 가지는 성질

### 대칭적 관계

한 원소가 다른 원소와 관계가 있다면, 반대로도 성립하는 성질

### 추이적 관계

첫 번째 원소가 두 번째 원소와 관계가 있고, 두 번째 원소가 세 번째 원소와 관계가 있을 때, 첫 번째 원소와 세 번째 원소도 관계가 있는 성질

### 이진관계

두 집합 A와 B의 원소들 사이의 관계를 나타내며, 순서쌍의 집합으로 표현됨

### 집합 위로의 이진관계

하나의 집합에서 정의되는 이진관계로, 같은 집합의 원소들 간의 관계를 나타냄

### 반사적인 관계

집합의 모든 원소가 자기 자신과 관계를 가지는 성질

### 대칭적 관계

한 원소가 다른 원소와 관계가 있다면, 반대로도 성립하는 성질

### 추이적 관계

첫 번째 원소가 두 번째 원소와 관계가 있고, 두 번째 원소가 세 번째 원소와 관계가 있을 때, 첫 번째 원소와 세 번째 원소도 관계가 있는 성질



## 5. 명제

> 참이나 거짓으로 명확히 판별될 수 있는 문장
> 

### 배타적 논리합(Exclusive Or)

- truth table

| p  | q |  $p \oplus q$  |
| --- | --- | --- |
| T | T  | F |
| T  | F  | T |
| F | T | T |
| F | F | F |

### 조건 명제

- truth table

| p  | q | p → q |
| --- | --- | --- |
| T | T  | T |
| T  | F  | F |
| F | T | T |
| F | F | T |

### 상호 조건 명제

- truth table

| p  | q | p ↔ q |
| --- | --- | --- |
| T | T  | T |
| T  | F  | F |
| F | T | F |
| F | F | T |

### 논리연산자 우선순위

() > NOT > AND > OR > → > ↔

### 논리적 동치

논리적 동치는 다음과 같은 수식으로 표현

$$
(p \rightarrow q) \equiv (\lnot p \lor q)
$$

$$
(p \rightarrow q) \equiv \lnot(p \land \lnot q)
$$

$$
p \leftrightarrow q \equiv (p \rightarrow q) \land (q \rightarrow p)
$$

### 드모르강의 법칙

논리식에서 부정(NOT)과 논리곱(AND), 논리합(OR)의 관계를 나타내는 법칙

$$
\lnot(p \land q) \equiv \lnot p \lor \lnot q
$$

$$
\lnot(p \lor q) \equiv \lnot p \land \lnot q
$$

### 동치법칙

- 교환법칙(Commutative Laws):

$$
p \land q \equiv q \land p
$$

$$
p \lor q \equiv q \lor p
$$

- 결합법칙(Associative Laws):

$$
(p \land q) \land r \equiv p \land (q \land r)
$$

$$
(p \lor q) \lor r \equiv p \lor (q \lor r)
$$

- 분배법칙(Distributive Laws):

$$
p \land (q \lor r) \equiv (p \land q) \lor (p \land r)
$$

$$
p \lor (q \land r) \equiv (p \lor q) \land (p \lor r)
$$

# 논리설계

## 1. 부울 대수(Boolean Algebra)

- 이진값(0과 1)만을 사용하는 대수 체계
- 컴퓨터의 논리 회로 설계에 필수적인 수학적 도구
- 디지털 시스템의 논리적 동작을 수학적으로 표현하고 분석하는 데 사용

### 부울식

부울 대수에서 사용되는 수식이나 표현을 부울식이라고 한다. 부울식은 다음과 같은 요소들로 구성된다:

- 상수: 0과 1
- 변수: 보통 영문자(x, y, z 등)로 표현
- 연산자: AND(· ), OR(+), NOT(')
- 연산자 우선순위: () > NOT > AND > OR

- 부울식 간소화 예시

$$
(A \land B) \land \lnot C => (A \cdot B) \cdot C'
$$

부울식의 간소화는 논리회로 설계에서 매우 중요한 과정이며, 카르노 맵이나 부울 대수의 법칙들을 이용하여 수행할 수 있다.

### 쌍대법칙(Principles of Duality)

> AND와 OR, 0과 1을 서로 교환하여 얻은 식도 동등하게 성립한다는 원리
> 
- AND(·)와 OR(+)를 서로 교환
- 0과 1을 서로 교환

예시:

$$
x + 0 = x \text{ 의 쌍대는 } x \cdot 1 = x
$$

$$
x + 1 = 1 \text{ 의 쌍대는 } x \cdot 0 = 0
$$

## 2. 부울함수

> 이진 입력값들을 이진 출력값으로 매핑하는 함수.
> 

 

### 주요 특징

- 입력값과 출력값이 모두 0 또는 1로 제한됨
- 진리표(Truth Table)를 통해 표현 가능
- 논리 게이트를 사용하여 구현 가능

### 기본적인 예시

- AND 함수: F(x,y) = x·y
- OR 함수: F(x,y) = x+y
- NOT 함수: F(x) = x'

부울 함수는 다음과 같은 형태로 표현될 수 있다:

```
F(x,y,z) = xy + z'
```

### 표준형

> 부울 함수를 표준화된 형태로 표현하는 방법
> 

**1. SOP (Sum of Products)**

최소항(minterm)들의 합으로 표현되는 형태로, 논리합(OR)의 형태이다.

- 각 항은 모든 변수를 포함하는 AND 항
- 각 변수는 반전되거나(NOT) 그대로의 형태로 나타남

```
F(x,y) = x'y + xy' + xy
```

**2. POS (Product of Sums)**

최대항(maxterm)들의 곱으로 표현되는 형태로, 논리곱(AND)의 형태이다.

- 각 항은 모든 변수를 포함하는 OR 항
- 각 변수는 반전되거나(NOT) 그대로의 형태로 나타남

```
F(x,y) = (x+y)(x'+y)(x+y')
```

두 표준형은 서로 변환이 가능하며, 회로 설계 시 상황에 따라 더 적합한 형태를 선택하여 사용한다.
