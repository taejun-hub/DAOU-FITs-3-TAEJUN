# 전처리기

## 1. `#include` - 헤더 파일 포함

```c
#include <stdio.h>   // 시스템 헤더
#include "myheader.h" // 사용자 정의 헤더
```

## 2. `#define`, `#undef` - 매크로 정의, 삭제

## 3. 조건부 컴파일

### `#if`, `#ifdef`, `#ifndef`, `#else`, `#elif`, `#endif`

```c
#define DEBUG

#ifdef DEBUG
    printf("디버그 모드\n");
#else
    printf("릴리즈 모드\n");
#endif
```

## 4. `#pragma` - 컴파일 전용 명령

- **특정 경고 무시**

```c
#pragma warning(disable : 4996) 
// Visual Studio에서 C 컴파일 시 발생하는
// "이 함수는 안전하지 않으므로 사용이 권장되지 않습니다" 라는 경고 무시
// scanf, fopen 등등
```

- **헤더 중복 포함 방지**

```c
#pragma once
```

---

# C언어에서의 모듈화

## 모듈화 방법

### 1. 헤더파일과 소스 파일의 분리

- 헤더파일(.h): 함수 선언 및 전역 변수 정의, 매크로, typedef
- 소스파일:(.c): 함수 실제 구현
- 함수 선언과 구현을 나누어 관리

### 2. 각 모듈마다 역할을 명확히 분리

- main.c - 되도록 다른 함수, 구조체, 변수를 포함하지 않는다(프로그램의 진입점 역할)
- domain.c, domain.h - 시스템 핵심 로직 역할
- db.h, db.c - 데이터베이스 연동
- ui.h, ui.c - 화면에 관련된 역할

### 3. 헤더 가드 사용

- #ifndef ~ #define ~ #endif

## 모듈화 기준

- 인터페이스와 구현 분리, 헤더파일과 소스파일 분리
- 높은 응집도와 낮은 결합도, 관련 기능끼리 묶고 모듈 간 의존성을 최소회
- 재사용성 고려 - 공통 기능은 별도의 모듈로 분리
- 변경 가능성 고려 - 확장 가능하도록 설계
