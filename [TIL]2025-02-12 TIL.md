# Layered Architecture (4계층)

### Presentation Layer

- 입출력에 관련된 code
- 주요 구성요소는 View 와 Controller

### Business (Logic) Layer

- 로직 처리
- 주요 구성요소는 Service 와 Domain Model

### Persistence Layer

- Database 처리
- 대표적인 구성요소는 Repository, DAO 등이 있다

### Database Layer

- oracle, mysql 등 실제 데이터베이스가 위치한 계층

![image.png](attachment:7b2632c0-65dd-4cfb-a28f-64e33c89bf3c:image.png)

# MVC Pattern

> 모델-뷰-컨트롤러(`model-view-controller`)는 소프트웨어 공학에서 사용되는 소프트웨어 디자인 패턴
> 

## 1. Model

### Data Model(Domain Model)

- 비지니스 로직에서 처리하는 데이터를 의미
- 도메인 모델 역할을 하는 클래스를 Domain Object, VO, DTO 라고 한다

### Service Model

- 도메인 객체를 이용하여 비지니스 로직을 처리
- 트랜잭션 담당
- DB처리는 `DAO`(Data Access Object) 로 분리하기도 한다.

## 2. View

- 입출력 담당(Presantation Layer)
- 유저 인터페이스
- 로직처리를 하지 않는다

## 3. Controller

- Model과 View 사이를 이어주는 브릿지 역할을 수행
- 사용자가 어플리케이션을 조작하여 발생하는 변경 이벤트들을 처리하는 역할

1. step 1 → 일반적인 DAO (중복 code 존재)
2. Step 2 → 중복된 code를 method로 분리(추출) → class의 유지 보수성
3. Step 3 → 추상 method 와 추상 class로 만들어서 제공 (`template method pattern`)
4. Step 4 → UserDAO의 변경해야 하는 부분을 별도의 code로 분리

# Template method pattern

> 상위 class에서 기본적인 logic의 흐름을 구현하고 하위 class에서 구체적인 구현 내용을 변경해서 구현하는 방법
> 

## 단점

- 단일 상속 문제 때문에 사용이 불가한 상황이 발생할 수 있다

# Dependency Injection

> 객체가 의존하는 또 다른 객체를 외부에서 선언하고 이를 주입받아 사용하는 것
> 

## DI 구현 방법

### 생성자 주입

- 객체가 생성될 때 필요한 의존성을 생성자를 통해 주입하는 방법
- 강한 의존성을 보장하며 불변성을 유지할 수 있다.

### 필드 주입

- 의존성을 객체의 필드에 직접 주입하는 방식
- 코드가 간결해지지만 가독성이 떨어지고 테스트가 어렵다.

### 세터 주입

- 객체가 생성된 후 필요한 의존성을 세터 메서드를 통해 주입하는 방법
- 유연하고 지연 주입이 가능하지만 의존성이 강제되지 않는다

## DI 장점

- 재사용성이 높아진다
- 의존성이 낮아진다
- 테스트 용이
- 가독성이 좋다
