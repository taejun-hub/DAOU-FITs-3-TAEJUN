# **TIL  - 2025.02.25**

## **1. 모의 주식 투자 페이지 디자인 구상**

- **토스 증권**을 참고하여 **모의 주식 투자 서비스**의 UI/UX를 설계
- figma를 통해 와이어 프레임 제작
- 주요 기능:
    - **메인 대시보드**: 총 자산, 투자 수익률, 보유 종목, 실시간 주가 변동
    - **주식 상세 정보**: 차트, 기업 정보, 최신 뉴스 제공
    - **매매 기능**: 슬라이드 방식의 직관적인 매수/매도 UI
    - **포트폴리오 분석**: 보유 종목 분석, 수익률 그래프 제공
    - **커뮤니티 & 랭킹**: 투자 성과 공유, 사용자 간 랭킹 시스템

## **2. Oracle DB 테이블 설계 및 수정**

### **테이블 생성 (Rooms, Orders, Portfolios)**

- `SEQUENCE`를 활용한 **자동 증가 `ID` 적용**
- `FOREIGN KEY` 및 `CHECK` 제약 조건 추가
- `TIMESTAMP DEFAULT SYSTIMESTAMP`로 **자동 날짜 기록**
- `ON DELETE CASCADE` 설정으로 **참조 무결성 유지**

```sql
CREATE TABLE Stocks (
    STOCK_CODE              VARCHAR2(10) PRIMARY KEY, 
    STOCK_NAME              VARCHAR2(60) NOT NULL, -- 종목명 (필수값)
    STOCK_TYPE              VARCHAR2(20), -- 종목 유형 (예: 보통주, 우선주)
    STOCK_MARKET            VARCHAR2(10) CHECK (STOCK_MARKET IN ('KOSPI', 'KOSDAQ', 'ETF', 'OTHER')), -- 시장 구분
    STOCK_LISTING_DATE      DATE, -- 상장일
    STOCK_IS_ACTIVE         NUMBER(1) DEFAULT 1 CHECK (STOCK_IS_ACTIVE IN (0, 1)), -- 상장 여부 (1=상장, 0=비상장)
    STOCK_SHARES_OUTSTANDING NUMBER DEFAULT 0, -- 총 발행 주식 수
    STOCK_CREATED_AT        TIMESTAMP DEFAULT SYSTIMESTAMP -- 생성일 (기본값: 현재시간)
);

CREATE SEQUENCE orders_seq
START WITH 1
INCREMENT BY 1
NOCACHE
NOCYCLE;

CREATE TABLE Orders (
    order_id              NUMBER DEFAULT orders_seq.NEXTVAL PRIMARY KEY, -- SEQUENCE 사용하여 자동 증가
    member_id             VARCHAR2(50) NOT NULL, -- 회원 ID (외래키 가능)
    account_number        VARCHAR2(255) NOT NULL, -- 계좌번호
    order_type            VARCHAR2(50) CHECK (order_type IN ('BUY', 'SELL')), -- 주문 유형 (매수/매도)
    order_total_quantity        NUMBER NOT NULL, -- 총 주문 수량
    order_price           NUMBER NOT NULL, -- 주문 가격
    order_status          VARCHAR2(50) CHECK (order_status IN ('PENDING', 'EXECUTED', 'CANCELLED', 'FAILED')), -- 주문 상태
    order_time            TIMESTAMP DEFAULT SYSTIMESTAMP, -- 주문 시간 (기본값: 현재 시간)
    order_execution_time  TIMESTAMP -- 체결 완료 시간 (NULL이면 미체결 상태)
);

CREATE SEQUENCE rooms_seq
START WITH 1
INCREMENT BY 1
NOCACHE
NOCYCLE;

CREATE TABLE Rooms (
    room_id              NUMBER DEFAULT rooms_seq.NEXTVAL PRIMARY KEY, -- 자동 증가 ID
    room_name            VARCHAR2(60) NOT NULL, -- 방 이름 (필수)
    room_type            VARCHAR2(60), -- 방 유형
    room_description     VARCHAR2(255), -- 방 설명 (길이 확장)
    room_status          VARCHAR2(20) CHECK (room_status IN ('ACTIVE', 'INACTIVE', 'CLOSED')), -- 방 상태 제한
    room_initial_seed    NUMBER, -- 초기 시드 머니 (숫자로 변경)
    room_created_by      VARCHAR2(60) NOT NULL, -- 방 생성자 ID
    room_start_date      DATE, -- 방 시작 날짜
    room_end_date        DATE, -- 방 종료 날짜
    room_created_at      TIMESTAMP DEFAULT SYSTIMESTAMP, -- 생성 시간 (자동 저장)
    room_updated_at      TIMESTAMP, -- 수정 시간
    room_deleted_at      TIMESTAMP -- 삭제 시간 (NULL이면 미삭제)
);
```
