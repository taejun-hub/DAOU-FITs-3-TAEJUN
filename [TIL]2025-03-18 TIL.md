# 메모리 구조

## Text Segment(코드 영역)

- 실행 코드(기계어 코드)저장
- 함수 코드, 상수 문자열 등이 저장
- 낮은 메모리 주소
- 읽기 전용(read-only)으로, 코드가 실행되는 동안 수정 불가
- 공유 가능

## Data Segment(데이터 영역)

> 정적(static) 데이터와 전역(global) 변수가 저장되는 영역
> 

### Initialized Data Segment

- 프로그램 시작 시 초기화
- 초기화된 전역 변수 및 정적 변수 저장

### BSS Segment

- 초기화 되지 않은 전역 변수 및 정적 변수 저장
- 실행 시 0으로 자동 초기화

## Heap(힙 영역)

- 동적 메모리 할당 영역(`malloc, calloc, realloc`)
- 크기가 런타임 시 결정되며, `free()`를 호출해야 해제됨

## Stack(스택 영역)

- 함수 호출 시 생성되는 지역변수, 매개변수, 반환 주소 등이 저장
- LIFO 방식으로 동작
- 함수 호출이 끝나면 자동으로 메모리가 해제됨
- 크기가 제한적이면, 너무 많은 메모리를 사용하면 스택 오버플로우가 발생
- 높은 메모리 주소

---

# 동적 할당

## `1. malloc()`

> 블록 내에 포함될 수 있는 어떤 종류의 데이터 객체에 대해서 적절히 정렬된 최소 size바이트를 갖는 메모리 블록의 포인터를 반환한다.
> 

### 기본형

```c
#include <stdlib.h>

void *malloc(size_t size)
```

- 32비트 모드에서 malloc은 주소가 항상 8의 배수인 블록을 리턴하고, 16비트 모드에서  malloc은 주소가 항상 16의 배수인 블록을 리턴한다.
- 반환값은 `void*` 타입이므로 적절한 형 변환 필요
- 메모리는 할당하지만 초기화는 하지 않는다.
- 메모리 할당에 실패하면,  `NULL`을 리턴하고, errno(오류의 코드가 담긴 변수)를 설정한다.
- 할당된 메모리는 `free()`를 호출하여 해제해야 함

### 사용법

```c
#include <stdio.h>
#include <stdlib.h>

// 기본형
void* malloc(size_t size);

int main() {
    int *arr = (int *)malloc(5 * sizeof(int));  // 정수형 배열(5개) 동적 할당
    if (arr == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }

    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);  // 초기화되지 않아서 쓰레기값 존재 가능
    }

    free(arr);  // 메모리 해제
    return 0;
}
```

---

## `2. calloc()`

> malloc과 유사하게 동적으로 메모리를 할당하며, 0으로 초기화된 블록을 반환한다.
> 

### 기본형

```c
#include <stdlib.h>

void *calloc(size_t cnt, size_t size)
```

- 다중 요소 할당 시 안전한 초기화가 필요할 때 사용
- malloc 에 비해 속도가 느리다

### 사용법

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int *arr = (int *)calloc(5, sizeof(int));  // 정수형 배열(5개) 동적 할당 및 0으로 초기화
    if (arr == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }

    for (int i = 0; i < 5; i++) {
        printf("%d ", arr[i]);  // 모든 값이 0으로 초기화됨
    }

    free(arr);  // 메모리 해제
    return 0;
}
```

---

## `3. realloc()`

> realloc 함수는 malloc을 통해 할당된 메모리의 크기를 축소하거나 확장하고 싶을 때 사용한다.
> 

### 기본형

```c
#include <stdlib.h>

void *realloc(void *ptr, size_t size)
```

- 크기를 동적으로 변경할 때 사용
- 메모리 크기를 변경하면서 새로운 메모리 주소를 반환할 수 있기 때문에, 변경된 포인터를 반드시 저장해야 함(기존의 메모리는 자동으로 free가 된다.)
- 사용 가능한 메모리가 부족하여 블록을 지정된 크기로 확장할 수 없는 경우, 원래 블록은 변경되지 않고 `NULL`을 반환힌다.
- 크기가 커지면, 새롭게 할당된 부분은 초기화 되지 않음
- 크기가 작아지면, 초과된 부분의 데이터 손실이 있을 수 있음
- size가 0일 경우, ptr가 가르키는 블록을 해제하고(`free`) `NULL`을 반환한다.
- 기존 ptr이 NULL 이면, `malloc(new_size)`와 동일하게 동작

### 사용법

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int *arr = (int *)malloc(3 * sizeof(int));  // 3개 정수 배열 할당
    if (arr == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }

    arr[0] = 1; arr[1] = 2; arr[2] = 3;

    int *new_arr = (int *)realloc(arr, 5 * sizeof(int));  // 크기를 5개로 확장
    if (new_arr == NULL) {
        printf("메모리 재할당 실패\n");
        return 1;
    }

    new_arr[3] = 4;
    new_arr[4] = 5;

    for (int i = 0; i < 5; i++) {
        printf("%d ", new_arr[i]);  // 새로 추가된 메모리는 초기화되지 않음
    }

    free(new_arr);  // 메모리 해제
    return 0;
}
```

## 4. 이차원 배열의 동적 할당

### **단일 포인터(`int*`)를 사용하여 1차원처럼 할당**

- **장점:** 메모리를 연속적으로 할당하여 캐시 효율이 좋음.
- **단점:** `arr[i][j]`처럼 사용하려면 **인덱스 변환 필요**.

```c
#include <stdio.h>
#include <stdlib.h>

int main() {
    int rows = 3, cols = 4;
    int *arr = (int *)malloc(rows * cols * sizeof(int));  //  1차원 배열로 2차원 할당

    if (arr == NULL) {
        printf("메모리 할당 실패\n");
        return 1;
    }

    // 값 저장 (2차원처럼 접근)
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            arr[i * cols + j] = i * cols + j;
        }
    }

    // 값 출력
    for (int i = 0; i < rows; i++) {
        for (int j = 0; j < cols; j++) {
            printf("%2d ", arr[i * cols + j]);
        }
        printf("\n");
    }

    free(arr);  //  메모리 해제
    return 0;
}

```

📌 **특징**

- `arr[i * cols + j]` 방식으로 2차원처럼 접근.
- **메모리가 연속적이라 빠름(캐시 최적화 효과).**
- **행(row) 포인터 배열이 필요 없음.**

### **이중 포인터(`int**`)를 사용하여 각 행을 별도로 할당**

- **장점:** `arr[i][j]` 형태로 사용 가능.
- **단점:** 각 행을 별도로 `malloc()` 호출해야 하므로 **메모리 단편화(fragmentation)가 발생할 수 있음.**

```c
c
복사편집
#include <stdio.h>
#include <stdlib.h>

int main() {
    int rows = 3, cols = 4;
    int **arr = (int **)malloc(rows * sizeof(int *));  // 행(row) 포인터 배열 할당

    for (int i = 0; i < rows; i++) {
        arr[i] = (int *)malloc(cols * sizeof(int));  // 각 행을 위한 메모리 할당
    }

    // 값 저장 및 출력
   for (int i = 0; i < rows; i++) {
		   for (int j = 0; j < col; j++) {
				   arr[i][j] = i * col + j;
				   printf("%d\t", arr[i][j]);
		   }
		   printf("\n");
   }

    // 메모리 해제
    for (int i = 0; i < rows; i++) {
        free(arr[i]);  // 각 행 메모리 해제
    }
    free(arr);  // 포인터 배열 해제

    return 0;
}

```

📌 **특징**

- `arr[i][j]`처럼 접근 가능하여 **직관적**.
- **각 행을 개별적으로 `malloc()` 할당**하여 관리.
- **메모리 해제 시 `free()`를 두 번 실행해야 함** (`arr[i]`, `arr`).
