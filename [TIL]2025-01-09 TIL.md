# 머신러닝(Machine Learning)

> 알고리즘을 이용하여 데이터의 패턴을 학습하고, 학습한 내용을 기반으로 스스로 판단하는 인공지는 기술
> 

## 1. 머신러닝 알고리즘의 종류

### 지도학습(Supervised Learning)

> 레이블이 있는 데이터를 사용하여 학습
> 
- 분류(Classification)와 회귀(Regression) 문제 해결
- 예: 스팸 메일 분류, 주택 가격 예측

### 비지도학습(Unsupervised Learning)

> 레이블이 없는 데이터에서 패턴을 발견
> 
- 군집화(Clustering)와 차원 축소에 활용
- 예: 고객 세그먼테이션, 이상 탐지

### 강화학습(Reinforcement Learning)

> 환경과의 상호작용을 통한 학습
> 
- 보상과 처벌을 통해 최적의 행동 방식 학습
- 예: 게임 AI, 로봇 제어

## 2. 분류(Classification)

> 기존에 있던 데이터를 이용하여 새로운 데이터를 분류하는 기계학습 방법
> 

### 분류 문제 유형

- 신용 평가
- 질병진단
- 얼굴인식
- automatic steering(차앞 이미지)

### 분류 알고리즘의 종류

- 의사결정트리
- 랜덤포레스트
- 선형판별분석
- K최근접이웃(K-Nearest Neighbor)
- 지원벡터머신
- 신경망
- 부스팅

### 로지스틱 회기분석

> 이진 분류 문제를 해결하는 데 주로 사용되며, 종속변수가 0 또는 1과 같은 이진값을 가질 때 적용된다.
> 
- 선형 회귀와 달리 확률을 예측하여 0과 1 사이의 값을 출력
- 시그모이드 함수(로지스틱 함수)를 사용하여 결과를 변환
- 해석이 용이하고 구현이 간단함

### 로지스틱 함수(Logistic Function)

> 로지스틱 함수는 시그모이드 함수(Sigmoid Function)라고도 하며, 입력값을 0과 1 사이의 값으로 변환하는 S자 형태의 함수
> 

$$
f(x) = \frac{1}{1 + e^{-x}}
$$

- 입력값이 음의 무한대에서 양의 무한대까지 가능
- 출력값은 항상 0과 1 사이의 값으로 제한됨
- 함수의 중간점(x=0)에서 출력값은 0.5
- 비선형 함수로, 이진 분류 문제에 적합

### 랜덤 포레스트

> 여러 개의 의사결정트리를 생성하고 각 트리의 예측을 결합하여 최종 결과를 도출하는 앙상블 학습 방법
> 
- 각 트리는 데이터의 무작위 하위 집합으로 학습됨
- 과적합을 방지하고 모델의 일반화 성능을 향상시킴
- 병렬 처리가 가능하여 대규모 데이터셋에 효과적
- 특성 중요도를 평가할 수 있음

**장점**

- 높은 예측 정확도
- 과적합에 강한 내성
- 다양한 유형의 데이터에 적용 가능
- 결측치 처리 능력이 우수

# 딥러닝(Deep Learning)

> 다중층 신경망을 이용하는 머신러닝 기술
> 

퍼셉트론 

- 가중치를 가진 여러 개의 입력으로부터 하나의 출력을 만들어내는 기계
- 인공신경망에서 인간의 신경에 해당

## 1. 합성곱 신경망

> 시각적 이미지를 분석하는 데 사용되는 깊고 피드-포워드적인 인공 신경망
> 

### 합성곱층

- 이미지 안에 있는 특성을 찾아내기 위해 컨볼루션 계산을 하는 층

### 발전순서

리넷 → 알렉스넷 → VGG-16 → 구글넷 → 레즈넷 → 모바일넷 → 이피션넷

## 2. 생성형 인공지능

> 방대한 양의 데이터로 사전 훈련된 인공신경망 모델을 기반으로 학습하지 않은 콘테츠를 만들어내는 인공지능
> 
- 생성형 AI는 인공지능의 한 분야로 신경망 기반의 다양한 아키텍처를 사용하여 작동
- 기존 데이터를 기반으로 새로운 내용을 생성하거나 변형하여 다양한 작업에 활요

### 생성형 AI 서비스 종류

- Chat GPT
- Gemini(구글과 딥마인드가 개발)
- 하이퍼 클로바X(네이버)
- KoGPT(카카오)

## 3. 초거대 언어모델(LLM)

> 방대한 양의 데이터를 기반으로 사전 학습된 딥 러닝 모델
> 

- 입력을 순차적으로 처리하는 RNN과 달리 전체 시퀀스를 병렬로 처리
- 인코더와 디코더가 이련의 텍스트에서 의미를 추출. 텍스트 내의 단어와 구누문 간의 관계를 이해하면서 자체 학습을 수행

### 인코더와 디코더

- 인코더는 입력 시퀀스를 하나의 벡터로 압축
- 디코더는 벡터를 통해 출력 시퀀스를 만듬
- 인코더가 입력 시퀀스를 하나의 벡터로 합축하는 과정에서 입력 시퀀스 정보의 일부가 손실될 수 있음(길이, 순서)

### 트랜스포머

- 기존의 구조인 인코더-디코더를 따르면서도 어텐션만으로 구현한 모델

### 어텐션 모델

- 입력 시퀀스 각 요소의 중요도를 측정하기 위해 소프트맥스 함수를 사용하여 어텐션 가중치를 계산
- 입력과 가중치 간의 가중합을 통해 출력이 생성
- 이러한 가중치 계산은 모델이 특정 요소에 집중하도록 유도

### LLM 한계점

- 일반적인 상황에서 프롬프트에 응답하는 데 탁월하나 훈련한 적이 없는 특정 영역에서는 성능이 떨어짐

 

## 4. 순환 인공 신경망(RNN)

- 신경망의 종류로 이전스텝의 결과물이 현재 스텝의 입력값이 됨
- 연속된 데이터를 처리하기 가장 좋은 모델
