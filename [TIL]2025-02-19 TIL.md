# Javascript

## Web 언어

- HTML - 내용, 구조
- CSS - 모양을 이쁘게
- Javascript - 동적 처리

## jQuery

### jQuery == Javascript 로 인해 생기는 코드 작성의 어려움

- 함수형 언어
- 객체 지향 언어
- prototype 기반 언어

### 장점

- 화면 제어가 쉽다
- event 처리가 쉽다
- 시각적인 처리가 쉽다(CSS)
- ajax 처리를 직관적으로 처리 가능

### 사용 방법

- `CDN`(Content Delivery Network) 방식 (따로 설치 필요 x)

[jQuery CDN – Latest Stable Versions](https://releases.jquery.com/)

- jQuery 객체: `$( ).method()`
- 괄호 안에는 문자열로된 `selector` 또는 문서 객체(`document object`) 가 온다

```html
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
    <!-- jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.7.1.min.js" integrity="sha256-/JqT3SQfawRcv/BIHPThkBvs0OEvtFFmqPF/lYI/Cxo=" crossorigin="anonymous"></script>
    <!-- user define JavaScript -->
    <script src="js/sample.js"></script>
</head>
<body>
    <!-- Element, Tag, Attribute -->
    <!-- Element 2가지 종류
         1. Block Level Element: Element 크기가 한 line을 차지
         2. Inline Element: Element 크기가 내용을 포함하는 정도만 차지
    -->
    <h1>소리없는 아우성</h1>  <!-- Block Level Element -->
    <div>
        <ol>
            <li>이순신</li>
            <li id="kang">강감찬</li>
            <li>신사임당</li>
            <li>홍길동</li>
        </ol>

    </div>
    <ul>
        <li id="seoul">서울</li>
        <li class="myClass">대구</li>
        <li>부산</li>
    </ul>
    <div>여기는 DIV 영역입니다.</div> <!-- Block Level Element -->
    <!-- 가장 쉬운 이벤트 처리 방법은 속성을 이용(inline 방식) -->
    <input type="button" value="클릭" onclick=myFunc() />
</body>
</html>
```

```jsx
function myFunc() {
    $('h1, div').css("color", "red");
    $('h1, div').remove();
    $('#seoul').css("color", "red");
    $('.myClass').css("background-color", "blue");
    $('ol > li').css("color", "green");
    $('div li').remove();
    alert($('#kang ~ li').text());
}

```

### Element

1. Block Level Element: Element 크기가 한 line을 차지
2. Inline Element: Element 크기가 내용을 포함하는 정도만 차지

### Seletor

- 전체 선택자: `$('*')`  ⇒ 모든 element 싹 다
- 타입 선택자: `$('태그명1, 태그명2')` ⇒ 원하는 tag 명을 이용해서 선택
- 아이디 선택자: `$('#아이디')`⇒ 원하는 id를 가진 element 선택(unique 한 선택)
- 클래스 선택자:  `$('.클래스명')` ⇒ 원하는 class를 가진 elelment 를 선택
- 자식 선택자: `$('태그 > 태그')` => > 으로 선택
- 후손 선택자: `$('태그  태그')` => 공백으로 선택
- 동위 선택자:
    - `$('(e1) + (e2)')` => 바로 다음에 나오는 형제 1개
    - `$('(e1) ~ (e2)')`~ => 바로 다음에 나오는 형제 포함 나머지 싹다
- 속성 선택자:
    - `$('[속성명 = 속성값]')`

### Method

- `.each()`
    - idx는 0,1,2,... for문의 첨자 역할
    - item => 선택된 문서 객체(Document Object) jQuery 객체가 아니다
    - jQuery 객체로 만들어 사용

```jsx
function myFunc() {
    $('[type=checkbox]:checked + span')
        .each((idx, item) => console.log($(item).text() + "가 선택되었습니다."));
}
```

## AJAX (Asynchronous JavaScript And XML)

> 웹 페이지를 새로고침 하지 않고도 서버와 비동기적으로 데이터를 주고 받는 기술
> 

### 동작흐름

1. 사용자 버튼 클릭 → AJAX 요청을 보냄(Javascript → 서버)
2. 서버가 요청을 처리하고 응답을 반환(서버 → Javascript)
3. Javascript가 응답 데이터를 받아 화면 일부 변경(DOM 업데이트)

### 예제

```jsx
function myFunc() {
    const kobisUrl = 'http://www.kobis.or.kr/kobisopenapi/webservice/rest/boxoffice/searchDailyBoxOfficeList.json';
    const key = '';
    let date = '20250218';

    $.ajax({
        async : true,                                   // 비동기화 여부 (default : true)
        url : kobisUrl,                                 // 요청할 서버 URL
        type : 'get',                                   // 타입 (get, post, put 등등)
        data : {
            key : key,
            targetDt : date,
        },
        dataType: 'json',                               // 서버가 보내주는 결과 데이터가 JSON(default: json)
        success : function(result) {              // 결과 성공 콜백함수
            console.log(result);
            let movieName = result.boxOfficeResult.dailyBoxOfficeList[0].movieNm;
            alert(`호출 성공`);
            $('h1').text(movieName);
        },
        error : function(request, status, error) { // 결과 에러 콜백함수
            console.log(error);
            alert("호출 실패");
        }
    })

}
```
