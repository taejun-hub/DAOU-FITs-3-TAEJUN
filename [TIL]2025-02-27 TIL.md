# 2025-02-27 TIL (Project)

## 1. JSP에서 현재 페이지에 따라 사이드바 활성화 처리

- `sidebar.jsp`는 `main.jsp`에서 `<jsp:include>`로 포함됨.
- `request.getRequestURI()`를 사용하면 `main.jsp`가 반환되므로 해결 필요.
- *각 JSP 페이지(`mainDashboard.jsp`, `roomRegister.jsp` 등)**에서 `request.setAttribute("currentPage", request.getRequestURI());` 설정.
- `sidebar.jsp`에서 `currentPage.endsWith('파일명')` 조건을 사용해 동적 스타일 적용.

## 2. 이미지 색상 변경

**CSS로 이미지 색상 변경 가능 여부**

**가능하지만 조건이 있음!**

1. **SVG 이미지** → `fill`, `filter`, `mask`, `mix-blend-mode` 사용 가능 
2. **PNG, JPG 같은 래스터 이미지** → `filter`를 활용한 색상 조절 가능 (완벽한 색상 변경은 어려움) 
3. **CSS `background-image` 사용 시 `opacity`, `mix-blend-mode` 조절 가능** 

![스크린샷 2025-02-27 174936](https://github.com/user-attachments/assets/386428fd-a3b8-4717-8933-b9fa133d7b57)


---
![스크린샷 2025-02-28 085239](https://github.com/user-attachments/assets/66aba70d-4990-46e8-a474-ee7f22e86051)

