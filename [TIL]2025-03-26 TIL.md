# 소켓 서버 개발

## 프로젝트 개요

- Visual Studio 2022 + C 언어 기반 TCP 서버 개발
- `select()` + 멀티 워커 스레드 구조로 **멀티 클라이언트 주문 처리 시스템** 구현
- Windows 이벤트, critical -section 등 활용

---

## Windows API 기반 소켓 연결 흐름 정리

### 전체 구조 요약

| 구성 요소 | 설명 |
| --- | --- |
| 서버 | `socket()`, `bind()`, `listen()`, `accept()` 사용해 클라이언트 연결 수신 |
| 클라이언트 | `socket()`, `connect()` → 서버에 주문 메시지 전송 |
| 통신 방식 | TCP 기반 스트림 통신 (`SOCK_STREAM`) |
| 감시 방식 | `select()` 사용으로 멀티 클라이언트 지원 |
| 동기화 | 워커 스레드는 Windows `CreateThread()` + `WaitForSingleObject()`로 대기 |

---

### 서버 소켓 생성 및 준비 단계 (WinAPI)

```c

WSADATA wsa;
WSAStartup(MAKEWORD(2, 2), &wsa);           // WinSock 초기화

SOCKET server = socket(AF_INET, SOCK_STREAM, 0); // TCP 소켓 생성

struct sockaddr_in addr = {0};
addr.sin_family = AF_INET;
addr.sin_port = htons(9000);                    // 포트 설정
addr.sin_addr.s_addr = INADDR_ANY;

bind(server, (struct sockaddr*)&addr, sizeof(addr));  // 주소 바인딩
listen(server, SOMAXCONN);                            // 연결 대기 시작

```

---

### 클라이언트 연결 수신 및 데이터 수신 (select 방식)

```c

fd_set master_set;
FD_ZERO(&master_set);
FD_SET(server, &master_set);      // 서버 소켓 등록

while (1) {
    fd_set read_fds = master_set;
    select(0, &read_fds, NULL, NULL, NULL);

    for (u_int i = 0; i < read_fds.fd_count; i++) {
        SOCKET s = read_fds.fd_array[i];

        if (s == server) {
            SOCKET client = accept(server, NULL, NULL);   // 클라이언트 접속 수락
            FD_SET(client, &master_set);                  // 클라이언트 소켓 추가
        } else {
            int len = recv(s, buf, sizeof(buf), 0);       // 데이터 수신
            ...
        }
    }
}

```

---

### 클라이언트 소켓 연결 흐름

```c

WSADATA wsa;
WSAStartup(MAKEWORD(2, 2), &wsa);         // 초기화

SOCKET sock = socket(AF_INET, SOCK_STREAM, 0); // TCP 소켓 생성

struct sockaddr_in server_addr;
server_addr.sin_family = AF_INET;
server_addr.sin_port = htons(9000);
InetPton(AF_INET, L"127.0.0.1", &server_addr.sin_addr); // IP 설정

connect(sock, (struct sockaddr*)&server_addr, sizeof(server_addr)); // 서버 연결

send(sock, "ORDER: BUY 100\n", strlen(msg), 0);        // 주문 메시지 전송
recv(sock, buffer, sizeof(buffer), 0);                 // 응답 수신

```

---

## 주요 WinAPI 정리

| 함수 | 설명 |
| --- | --- |
| `WSAStartup()` | WinSock 환경 초기화 |
| `socket()` | 소켓 생성 (TCP or UDP) |
| `bind()` | 서버 측 IP/포트 바인딩 |
| `listen()` | 연결 대기 시작 |
| `accept()` | 클라이언트 연결 수락 |
| `connect()` | 클라이언트 → 서버 연결 |
| `recv()` | 수신 |
| `send()` | 송신 |
| `select()` | 여러 소켓의 상태 감시 |
| `closesocket()` | 소켓 닫기 |
| `FD_SET/FD_CLR/FD_ZERO` | 소켓 셋 조작 |
| `CreateThread()` | 워커 스레드 생성 |
| `WaitForSingleObject()` | 이벤트 or 큐 대기 |

---

---

## 서버 구조 요약 다이어그램

```css
[Client 1] ---┐
[Client 2] ---┤              ┌────────────┐
[Client N] ---┴── TCP ──────►│ 서버 소켓   │
                             └─────┬──────┘
                                  ▼
                      [select()로 감시 + recv()]
                                  ▼
                          [공유 주문 큐 enqueue]
                                  ▼
                          [Worker Thread들 처리]
```

## 오늘 해결한 주요 이슈 정리

| 이슈 | 원인 | 해결 방법 |
| --- | --- | --- |
| `select()` 이벤트 감지 안 됨 | `FD_SETSIZE` 제한으로 0~63만 검사 | `read_fds.fd_array[]` 직접 순회로 변경 |
| `recv()` -1 반복 + 소켓 0 이벤트 반복 | 죽은 소켓이 FD_SET에 남아 있음 | `recv()` 실패 시 `closesocket()` + `FD_CLR()` 필수 |
|  |  |  |
| `boolean` 재정의 오류 | Windows 헤더와 충돌 (`rpcndr.h`, `wtypesbase.h`) | `#define WIN32_LEAN_AND_MEAN`  |
| lock-free ring buffer,  | Visual Studio는 C11 atomic 미지원 | 배열 큐와 CriticalSection 이용 |
|  |  |  |
|  |  |  |

---

## 중요 개념 정리

### select() 이벤트 감지 시 유의사항

```c

// 올바른 방식
for (u_int i = 0; i < read_fds.fd_count; i++) {
    SOCKET s = read_fds.fd_array[i];
}

```

### recv() 실패 처리 예시

```c

int len = recv(s, buf, sizeof(buf), 0);
if (len <= 0) {
    printf("recv 실패: %d, WSA error: %d\n", len, WSAGetLastError());
    closesocket(s);
    FD_CLR(s, &master_set);
}

```

---

## 테스트 방식

- `CreateThread()`로 클라이언트 10개 생성
- 각 클라이언트가 `"ORDER: BUY XX\n"` 메시지 전송
- 서버에서 처리 후 응답 `"ORDER RECEIVED\n"`

---
