# 구조체

## 1. 구조체 기본 개념

- 여러 개의 변수를 하나의 데이터 단위로 묶어 관리할 수 있도록 해주는 사용자 정의 자료형
- 구조체로 만들어진 자료는 연속된 메모리에 할당된다

```c
struct 구조체이름 {
    데이터형 변수명1;
    데이터형 변수명2;
};
```

## 2. 구조체 선언 및 사용

```c
	// 방법 1.
	struct student {
		char name[12];
		int age;
		char phone[12];
	};
	struct student s1;

	// 방법 2.
	struct student2 {
		char name[12];
		int age;
		char phone[12];
	} s2;

	// 방법 3 => 가장 많이 사용
	typedef struct student3 {
		char name[12];
		int age;
		char phone[12];
	} ST;
	ST s3;
```

## 3. 구조체 응용

### 함수와 구조체

- 함수의 인수로 구조체를 직접 전달할 수 있다
- 함수의 인수로 구조체를 사용하려면 구조체를 전역 변수 선언 영역에 선언 해야 한다
- 함수의 인수로 구조체가 전달되면 구조체의 모든 멤버가 복사되어 함수로 전달된다
- 즉 구조체는 하나의 자료형으로 취급되기 때문에 주소에 의한 호출이 아니라 값에 의한 호출로 처리된다

### 구조체 포인터

- 포인터(`ptr`)를 사용하여 구조체 멤버에 접근할 때는 `>` 연산자 사용
- `ptr->id`는 `(*ptr).id`와 동일한 의미

```c
#include <stdio.h>

typedef struct {
    int id;
    char name[20];
    float price;
} Product;

int main() {
    Product p1 = {1, "Headphones", 199.99};
    Product *ptr = &p1;  // 구조체 포인터 선언

    // 포인터를 이용한 구조체 멤버 접근
    printf("ID: %d\n", ptr->id);  // ptr->필드명 사용
    printf("Name: %s\n", ptr->name);
    printf("Price: %.2f\n", ptr->price);

    return 0;
}
```
