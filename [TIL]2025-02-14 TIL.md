# 기본 개념(용어)

## 1. Internet(인터넷)

> 전 세계의 컴퓨터와 네트워크가 서로 연결된 정보 통신망
> 
- 인터넷은 네트워크 자체를 의미하고, 웹은 인터넷 위에서 동작하는 서비스 중 하나
- 분산형 네트워크
- 다양한 서비스 제공 - 웹, 이메일, FTP, 클라우드

## 2. IP Address

- IP 주소는 네트워크에서 장치를 식별하는 고유한 숫자 주소(논리적 주소)
- IPv4(32비트) 와 IPv4(128비트)로 구분
- DNS를 통해 IP 주소를 사람이 이해하기 쉬운 도메인으로 변환

## 3. DNS (Domain Name System)

- 도메인 → IP 주소 변환

## 4. Port

- 특정 IP 주소에서 실행되는 여러 프로그램을 구분할 수 있게 해주는 것이 포트 번호

## 5. WWW(World Wide Web)

- 인터넷에 연결된 컴퓨터들을 사람들이 정보를 공유할 수 있는 전세계적인 정보 공간
- 하이퍼링크를 따라 이동하는 행위를 웹 서핑 혹은 웹 브라우징이라고 한다.
- 위와 관련된 내용들이 모여있는 웹 페이지들의 집합을 웹 사이트라고 한다

## 6. HTTP

- `stateless protocol` - 필요할 때마다 연결하고 데이터를 받아오자 마자 바로 연결을 끊어버리는 방식이 바로 HTTP 프로토콜의 연결 메커니즘
- 클라이언트와 서버 사이에 이루어지는 요청/응답(`request`/`response`) 프로토콜

## 7. Web Service

### Static Web

- 클라이언트 요청에 정적 resource 를 가지고 응답을 돌려준다

### Dynamic Web

- 클라이언트가  request (프로그램 실행) 를 보내고 서버가  WAS 에 전달하여 처리

<aside>
✅

WAS( web application server) ⇒ 웹 서버와 어플리케이션 간의 중간 역할을 하며, 웹 요청을 받아 DB와 연동하여 동적인 콘텐츠 생성

</aside>

# Servlet

- 클라이언트 요청이 들어오면 sevlet conta

```java
package org.example.hellosevletproject;

import jakarta.servlet.ServletConfig;
import jakarta.servlet.ServletException;
import jakarta.servlet.annotation.WebServlet;
import jakarta.servlet.http.HttpServlet;
import jakarta.servlet.http.HttpServletRequest;
import jakarta.servlet.http.HttpServletResponse;

import java.io.IOException;
import java.io.PrintWriter;

@WebServlet(value="/myservlet")
public class MyHello extends HttpServlet {

    MyHello() {}

    // 기본적으로 overriding 해야하는 method 가 있다

    // 생성자 역할을 한다(객체가 만들어질 때 MyHello 초기화)
    @Override
    public void init(ServletConfig config) throws ServletException {
        // Servlet 초기화 method
        super.init(config);
        System.out.println("init 호출");
    }

    @Override
    protected void service(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        super.service(req, resp);
        System.out.println("service 호출");
        // 일반적으로 service 는 overriding 하지 않는다. (이미 하는 일이 잘 만들어져 있기 때문)
        // 클라이언트 요청이 GET 방식  -> doGet() 호출
        // 클라이언트 요청이 POST 방식 -> doPost() 호출
    }

    @Override
    protected void doGet(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        // Client가 Get 방식으로 요청했을 떄 doGet이 호출된다

        // 클라이언트에게 돌아가는 출력 내용은 인자로 들어오는 resp 를 이용해서 처리하면 된다

        // 1. 클라이언트에게 연결된 데이터 통로를 뚫는다
        PrintWriter out = resp.getWriter();
        out.println("<html><head></head><body>");
        out.println("<h1>Hello World</h1>");
        out.println("</body></html>");
        out.close();
    }

    @Override
    protected void doPost(HttpServletRequest req, HttpServletResponse resp) throws ServletException, IOException {
        super.doPost(req, resp);
    }

    // doGET() 과 doPost() 가 끝나면 thread 는 사라지고 MyHello 인스턴스는 남아있다.
}

```

## Servlet 실행

[`http`://`localhost`:`8080`/`HelloSevletProject_war_exploded`](http://localhost:8080/HelloSevletProject_war_exploded) /`myservlet`

1. protocol ⇒ http
2. 서버쪽 컴퓨터의 ip ⇒ localhost
3. 웹 서버의 포트번호 ⇒ 8080
4. 프로젝트의 context ⇒ /HelloSevletProject_war_exploded
5. 해당 프로젝트의 Servlet에 대한 url mapping ⇒ /myservlet
