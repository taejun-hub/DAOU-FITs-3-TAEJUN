## 파일의 끝 확인하기

### `fscanf()` 함수의 반환값을 활용

```c
while( ( fscanf(fp, “%d”, &age ) != EOF ) {
		printf(“%2d”, age);
}
```

- 읽을 데이터가 없다면 -1 반환
- 성공적으로 읽은 데이터 개수 반환

### `feof()`함수 이용

```c
int feof(FILE *fp);
```

- 파일의 끝(EOF)에 도달하면 0이 아닌 값을 반환
- 아직 읽을 데이터가 있다면 0을
- 파일의 데이터를 읽기 전에 파일의 끝에 도달했는지를 검사하는 것이 아니라 먼저 데이터를 읽은 후에 확인해야 한다.

## 이진파일에 데이터 쓰기/읽기

### `fwrite()` 함수

> 바이너리 파일 또는 텍스트 파일에 바이너리 데이터를 저장하는 함수
> 

- **기본형**

```c
size_t fwrite(const void *ptr, size_t size, size_t count, FILE *stream);
// ptr => 저장할 데이터의 주소
// size => 데이터 한 개의 크기(바이트)
// count => 저장할 데이터 개수
// stream => 파일 포인터
```

- 성공 시 count 개수만큼 저장된 개수 반환
- 실패 시 저장된 개수보다 작은 값 또는 0 반환
- 예제

```c
#include <stdio.h>

typedef struct {
    int id;
    char name[20];
    double balance;
} Customer;

int main() {
    FILE *file = fopen("customers.dat", "wb");  // 바이너리 모드로 파일 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다.\n");
        return 1;
    }

    Customer customers[] = {
        {1001, "김철수", 50000.00},
        {1002, "이영희", 75000.50},
        {1003, "박민수", 120000.75}
    };

    size_t count = fwrite(customers, sizeof(Customer), 3, file);  // 구조체 배열 저장
    printf("저장된 고객 정보 개수: %zu\n", count);

    fclose(file);
    return 0;
}
```

### `fread()` 함수

> 바이너리 파일에서 데이터를 읽어오는 함수
> 
- **기본형**

```c
size_t fread(void *ptr, size_t size, size_t count, FILE *stream);
```

- 성공 시  `count` 개수만큼 정상적으로 읽은 개수를 반환
- 실패 시 읽은 개수보다 작은 값 또는 `0` 반환
- 첫 인수인 데이터 저장 시작 주소에 해당하는 기억장소는 (블록크기 * 블록개수)바이트의 데이터를 저장하기 위해 충분한 기억장소여야 한다.

## 파일에 임의 접근하기

### `fseek()` 함수

> 파일 포인터를 원하는 위치로 이동하는 데 사용하는 함수
> 
- **기본형**

```c
int fseek(FILE *stream, long offset, int origin);
```

- origin 값
    - `SEEK_SET` : 파일의 시작 지점(0)
    - `SEEK_CUR` : 파일의 현재 지점(1)
    - `SEEK_END` : 파일의 끝 지점(2)
- offset ⇒ 기준점에서 몇 바이트 이동할지 나타낸다 기준점 이전이라면 음수, 이후라면 양수
- 이동을 성공하면 0을 반환하고, 실패하면 0이 아닌 값을 반환한다

### `ftell()` 함수

> 파일 포인터 변수에 연결된 파일에서 다음 읽기/쓰기 위치를 파일의 시작지점으로 이동
> 
- **기본형**

```c
long ftell(FILE *stream);
```

- 성공 시 현재 파일 포인터 위치 (바이트 단위) 반환
- 실패 시 -1L 반환

### `rewind()` 함수

> 파일 포인터에 연결된 파일의 다음 읽기/쓰기 위치를 파일의 시작 지점으로 이동
> 
- **기본형**

```c
void rewind(FILE *stream);
```
