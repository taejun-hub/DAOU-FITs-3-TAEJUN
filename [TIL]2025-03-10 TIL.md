# 함수

## define

### 매크로 상수

- `#define [매크로명]  [실제값]`

### 매크로 함수

- 일반함수처럼 인수를 갖는다
- 컴파일 속도가 빨라진다
    - why? 컴파일 할 때 함수가 호출되는 개념이 아니라 호출된 곳에 코드가 직접 삽입되기 때문
- 매크로 함수를 정의할 때 치환할 내용에 포함된 모든 인수를 ( )로 묶어줘야 한다
    - why? 연산자 우선순위 문제로 잘못된 결과가 발생 가능

## 문자열 관련 함수

### **`strcpy()` - 문자열 복사**

**✅ 역할**

- `strcpy()` 함수는 **문자열을 복사**할 때 사용됩니다.
- `dest`(대상) 배열에 `src`(원본) 문자열을 복사합니다.
- **널 문자(`\0`) 포함하여 복사**되므로, 기존 문자열이 있다면 덮어씁니다.

**📌 함수 원형**

```c
char *strcpy(char *dest, const char *src);
```

- `dest` → 문자열이 복사될 대상(버퍼)
- `src` → 복사할 원본 문자열
- 반환값: `dest` 포인터 반환

---

**⚠ 주의할 점 (버퍼 오버플로우 위험)**

- `dest`의 크기가 `src`보다 작으면 **메모리 오버플로우 발생 가능**
- 이를 방지하려면 **`strncpy()` 사용**이 더 안전함

---

### **`strcmp()` - 문자열 비교**

**✅ 역할**

- `strcmp()` 함수는 **두 개의 문자열을 비교**
- **아스키 코드 값을 기준**으로 비교하며, 결과는 정수(`int`) 값으로 반환

**📌 함수 원형**

```c
int strcmp(const char *str1, const char *str2);
```

- `str1`, `str2`: 비교할 두 문자열
- 반환값:
    - `0` → 두 문자열이 같음
    - `< 0` → `str1`이 `str2`보다 사전순으로 앞에 있음
    - `> 0` → `str1`이 `str2`보다 사전순으로 뒤에 있음

---

---

**📌 `strcmp()` 비교 원리**

- **문자별로 비교하여 가장 먼저 다른 문자가 나오는 위치에서 비교 종료**
- **아스키 코드 기준 비교** → 알파벳, 숫자, 특수문자 정렬 가능

```c
strcmp("apple", "banana");  // 결과: 음수 (a가 b보다 앞)
strcmp("hello", "hello");   // 결과: 0 (같음)
strcmp("zebra", "apple");   // 결과: 양수 (z가 a보다 뒤)
```

---

**⚠ 주의할 점**

- 대소문자를 구분하여 비교 (`strcmp("Hello", "hello")` 결과 **음수**)
- **대소문자 무시하려면** `strcasecmp()`(Unix), `_stricmp()`(Windows) 사용

```c
strcmp("Hello", "hello");   // 결과: 음수 (대문자가 소문자보다 앞)
strcasecmp("Hello", "hello");  // 결과: 0 (대소문자 구분 없이 비교)
```

---

# 배열

> C 언어에서 배열(Array)은 같은 데이터 타입의 여러 개의 값을 **연속된 메모리 공간**에 저장하는 자료구조
> 

---

## **1. 배열 선언 및 초기화**

```c
int arr[5] = {1, 2, 3, 4, 5};  
```

- **초기값을 모두 지정하지 않으면 나머지는 `0`으로 초기화됨**

```c
int arr[5] = {1, 2};  // {1, 2, 0, 0, 0}
```

- **크기 생략 가능 (초기화 값 개수에 맞춰 자동 설정)**

```c
int arr[] = {10, 20, 30};  // 크기 자동 설정 (3)
```

---

---

## **2. 다차원 배열**

배열을 **2차원, 3차원** 등 다차원으로 선언할 수도 있습니다.

### **2차원 배열 선언 및 초기화**

```c
int matrix[2][3] = {
    {1, 2, 3},
    {4, 5, 6}
};
```

- 크기 생략 가능:

```c
int matrix[][3] = { {1, 2, 3}, {4, 5, 6} };
```

---

## **배열과 포인터**

### **배열이름은 주소값**

```c
int arr[3] = {10, 20, 30};
printf("%p\n", arr);    // 배열 시작 주소 출력
printf("%p\n", &arr[0]); // 같은 주소 출력
```

### **포인터로 배열 접근**

```c
int *ptr = arr;  // 배열 이름은 배열의 첫 번째 요소 주소
printf("%d\n", *(ptr + 1));  // arr[1] 출력 (20)

//  arr[i]는 *(arr + i)와 동일!
```

---

## **문자열 배열 (`char 배열`)**

C에서 문자열은 **널 문자(`\0`)로 끝나는 `char` 배열**로 저장됩니다.

### **문자열 배열 선언 & 초기화**

```c
char str1[] = "Hello";  // 자동으로 '\0' 포함됨
char str2[6] = "Hello";  // 수동 크기 지정
```

### **`printf()`로 문자열 출력**

```c

printf("%s\n", str1);  // Hello
```

---

## **배열을 함수에 전달**

배열은 함수에 전달될 때 **포인터처럼 전달**됩니다.

### **배열을 함수에 전달하는 방법**

```c
// **배열의 크기는 함수 내에서 알 수 없으므로** 반드시 size를 같이 전달
void printArray(int arr[], int size) {
    for (int i = 0; i < size; i++) {
        printf("%d ", arr[i]);
    }
    printf("\n");
}

int main() {
    int nums[] = {1, 2, 3, 4, 5};
    printArray(nums, 5);  // 배열 전달
    return 0;
}
```
