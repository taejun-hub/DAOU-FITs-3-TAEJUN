# Servlet

## 구현 과정

1. web project 생성
    1. 버전 동기화 ( JDK 버전, tomcat 버전, Jakarta EE 버전)
2. Tomcat을 이용하여 Deploy를 할 수 있다
    1. 이때 application context를 수정해서 사용할 수 있다.
3. Servlet이라고 불리는 Java program을 작성해서 web client가 호출해서 그 결과를 browser를 통해서 받아보려면 어떻게 해야할까?
    1. Servlet은 interface이다. 
    2. Servlet → GenericServlet class → HttpServlet class
    3. Servlet interface를 직접적으로 구현하는게 아니라 HttpServlet을 상속해서 Servlet 구현
    4. 상속되는 method가 존재
        1. `init()` : Servlet 객체가 생성될 때 호출되고, 해당 Servlet을 초기화 한다.(생성자 대신 init 이용)
        2. `service()` : 클라이언트가 Servlet을 실행할 때 호출되는 method, 이 method는 request 가 GET 방식이면 doGet()을 호출하고, POST 방식이면 doPost()를 호출하는 것으로 구현되어 있다. (일반적으로 overriding 하지 않는다)
        3. `doGet(), doPost()` :  이 두개의 method를 overriding 해서 클라이언트가 원하는 작업을 수행할 수 있다.
        4. `destroy()` : Servlet 인스턴스가 WAS에서 삭제될 때 수행되는 method ⇒ 사용하고 있는 여러 resource들을 해제
    5. 클라이언트가 GET 방식으로 Servlet을 호출했을 때 간단하게 response를 전송하는 Servlet을 구현
        1. Servlet에 논리적인 이름을 하나 부여해서 그걸 클라이언트가  이용해서 Servlet을 호출할 수 있도록 해야 한다.
            1. `XML 파일`을 이용해서 설정할 수 있다. (XML 을 이용하기 때문에 번거롭지만, 전체적인 이름 관리를 하나의 파일에서 할 수 있어서 관리에 좋다)
            2. `annotation`을 이용해서 설정 가능 (일반적인 방식)
        2. doGet() 에서 클라이언트에게 내용 전달
            1. `content type` 설정
            2. `PrintWriter`를 이용해서 클라이언트에게 데이터 연결 통로를 연다.
            3. `println`을 통해 전달
    6. 클라이언트가 Servlet에게 데이터를 전달하는 방법
        1. GET 방식
            1. Query String을 이용해서 데이터를 전달
            2. Query String ⇒ 특수한 형식을 가진 문자열  `?변수1=값1&변수2=값2`
            3. 전달된 데이터는 `getParameter(”변수명”)` 을 통해 받을 수 있다
            4. 이 방식은 보안성 문제가 있다. (128bit 암호화 필요)
            5. URL에 길이 제한이 있기 때문에 길이가 긴 데이터 전달이 어렵다
        2. POST 방식
            1. 보안성이 약간 있다(브라우저 주소창에 변수와 값에 대한 내용이 표현되지 않는다)
            2. request header에 데이터가 포함되어 전달되기 때문에 길이에 제한이 없다
            3. HTML의 특수한 태그를 이용해서 서버(Servlet)를 호출하는 구조
            4. 사용자 입력 양식을 표현하는 HTML 작성 필요
