# 변수

## 지역변수, 자동변수(auto)

- 중괄호 내부, 함수의 매개변수에서 사용되는 변수
- 스택 메모리에 저장
- 블록 종료 시 변수의 메모리 공간이 소멸 된다.
- 지역변수는 초기화 하지 않으면 컴파일 에러가 나거나 쓰레기 값이 저장된다.

## 전역변수(global)

- 중괄호 외부에서 선언되는 변수
- 어느 지역에서는 참조할 수 있고, 프로그램 종료 전에 메모리가 소멸되지 않는다
- 초기화하지 않으면 기본값인 0으로 자동 초기화 된다.
- 메모리 데이터 영역에 저장된다.

## 정적변수(static)

- 같은 파일 내에서면 사용 가능
- 데이터 영역에 저장되며, 프로그램 종료까지 유지된다.
- 보안성 증가(외부 접근 차단)
- **정적 전역 변수**
    - 일반적인 전역 변수는 모든 파일에서 접근 가능하지만, 정적 전역 변수는 선언된 파일에서만 접근 가능
- **정적 지역 변수**
    - 정적 전역 변수와 다른 함수에서 접근할 수 없음 (블록 내에서만 접근 가능)

## 외부변수(extern variable)

- 한 파일에서 정의하고 다른 파일에서 참조할 수 있는 변수
- extern 키워드를 사용하면 다른 파일에서 선언된 전역 변수를 참조 가능
- `extern int globalVar;`  → 다른 파일에서 선언된 globalVar 변수를 참조한다

## 레지스터 변수(register variable)

- CPU 레지스터에 저장될 가능성이 있는 변수(보장되지 않는다)
- 자주 사용하는 변수를 메모리가 아니라 레지스터에 저장하여 접근 속도를 높이는 것
- 포인터로 접근할 수 없다(&연산자 사용 불가)

# 포인터

## 1. 포인터의 기본 개념

> 포인터는 메모리 주소를 저장하는 변수
> 

**📌 변수에 저장된 값을 출력하는 경우 실제로 컴퓨터에서 어떻게 동작하나?**

- **1단계**: 기계어로 번역되어 실행파일(exe)로 저장된다.
- **2단계**: loader에 의해 실행 파일이 메모리에 적재되면서 실행된다.
- **3단계**: 모든 변수는 컴파일러에 의해 생성되는 `심벌테이블`에 저장되고 심벌테이블에서 변수명과 메모리 주소(값이 저장된 주소)가 저장된다.
- **4단계**: 프로그램이 실행될 때 모든 변수는 심벌 테이블의 있는 주소에 의해 접근한다.

### 포인터의 필요성

- 메모리를 직접 제어할 수 있고, 효율적인 자료구조를 쉽게 사용할 수 있다.
- 변수의 주소를 직접 조작이 가능하고, 메모리의 성능과 효율성을 향상 시킬 수 있다.

### 포인터의 연산

| 연산 | 설명 |
| --- | --- |
| `*ptr` | 포인터가 가리키는 값(역참조) |
| `ptr++` | 다음 메모리 주소로 이동 |
| `ptr--` | 이전 메모리 주소로 이동 |

**✅포인터 연산 예제**

```c
#include <stdio.h>
int main() {
    int num = 10;
    int *ptr = &num;

    printf("ptr의 값 (주소): %p\n", ptr);
    ptr++;  // 포인터 증가
    printf("ptr 증가 후 주소: %p\n", ptr);

    return 0;
}
```

- **`ptr++` 하면 `sizeof(int)`(4바이트)만큼 주소가 증가함**.

## 2. 이중 포인터(Double Pointer, **)

> 포인터 변수의 주소를 저장하는 변수
> 

### 이중 포인터와 배열

| **이중 포인터 (`int **ptr`)** | 포인터의 주소를 저장 |
| --- | --- |
| **포인터 배열 (`int *arr[]`)** | 여러 개의 포인터를 저장하는 배열 |
| **2차원 배열 (`int arr[][]`)** | 행과 열을 가진 정적인 배열 |

**`int **ptr` vs `int *ptr[]` 차이**

| 선언 | 설명 |
| --- | --- |
| `int **ptr` | 포인터를 가리키는 포인터 |
| `int *ptr[]` | 포인터 배열 (여러 개의 `int *` 저장) |

**📌 다중 포인터를 사용하는 이유**

- 배열과 문자열의 조작에 포인터를 사용하므로써 간결하고 효율적인 처리 가능
- 배열의 첨자보다 포인터를 사용하는 것이 더 빠른 처리 가능
- 동작 자료의 처리나 순환 자료 구조의 표현, 참조에 의한 호출 등에서 포인터를 사용

## 3. 포인터와 배열

- 배열의 이름은 포인터 변수와 같은 역할
- 배열의 이름은 배열 첫 번째 요소의 주소 상수값을 가진다

### 배열의 이름과 포인터 변수

- 두 방법의 차이
    - 배열의 이름을 나타내는 변수는 기억장소에 저장되지 않는다
    - 별도의 포인터 변수를 선언하여 사용하는 것은 포인터 변수 자체가 메모리에 저장
    
    ```c
     char s[9] = "swimming";
     char* t = "야구";
     // 동일한 주소 출력
     printf("\ns가 저장된 주소: %p", s); 
     printf("\ns의 저장된 주소: %p", &s);
    ```
    
    **`s`와 `&s`의 타입 차이**
    
    | 표현 | 의미 | 타입 |
    | --- | --- | --- |
    | `s` | **배열의 첫 번째 요소 주소** (`&s[0]`) | `char *` |
    | `&s` | **배열 전체의 주소** | `char (*)[9]` |
    
    **📌  차이점**
    
    1. **주소 값은 같지만, 타입이 다름**
        - `s`는 `char *` (배열의 첫 번째 요소 주소)
        - `&s`는 `char (*)[9]` (배열 전체의 주소)
    2. **포인터 연산에서 차이가 남**
        - `s + 1` → `char *` 타입이므로 **한 글자(1바이트)씩 이동**
        - `&s + 1` → `char (*)[9]` 타입이므로 **9바이트(배열 크기만큼) 이동**
