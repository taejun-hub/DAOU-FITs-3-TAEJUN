# 인덱스

- 데이터베이스 테이블의 검색 성능을 향상시키기 위해 사용되는 특별한 구조
- 도서관 책 목차와 같은 역할
- 특정 데이터를 빠르게 찾아갈 수 있는 경로를 제공

## 1. 사용 목적

- 검색 성능 향상
- 쿼리 실행 시간 단축
- 테이블의 특정 컬럼을 기준으로 데이터 정렬 및 관리

## 2. 동작 원리

### 테이블 스캔(Table Full Scan)

- 인덱스가 없는 경우 테이블의 모든 데이터를 처음부터 끝까지 읽어야 함
- 데이터 양이 많아질수록 성능 저하 발생

### 인덱스 스캔(Index Scan)

- 인덱스를 사용하면 필요한 데이터가 저장된 위치를 빠르게 탐색 가능
- 테이블의 일부만 읽기 때문에 성능 향상

## 3. 인덱스 구조

- B-Tree 인덱스(가장 일반적인 구조)
- 균형 이진 트리 형태로 데이터 검색에 최적화
- 루트 노드 → 중간 노드 → 리프 노드 순서로 탐색
- 리프 노드에는 실제 데이터 위치 정보 저장

## 4. 인덱스 비용

1. 수직적 탐색
2. 수평적 탐색
3. 테이블 Ranodom Access
- Cost
    
    3  >  1  >   2
    

## 5. 인덱스 장점

- 검색 속도 향상
- 정렬된 데이터 관리
- 대용량 데이터 처리

## 6. 인덱스 단점

- 추가 저장 공간 필요 → 인덱스는 테이블 외에도 별도의 공간을 차지
- 쓰기 작업 성능 저하 → INSERT, UPDATE, DELETE 작업 시 인덱스도 함께 갱신
- 인덱스 과다 생성 문제  → 너무 많은 인덱스는 관리 비용 증가와 성능 저하를 초래

## 7. 인덱스 유형

### 단일 컬럼 인덱스

- 하나의 컬럼만 기준으로 인덱스를 생성
- 고객 ID, 제품 코드 등 단일 검색 기준

### 결합 인덱스

- 두 개 이상의 컬럼을 조합하여 생성
- 이름 + 생년월일, 제품 + 카테고리

### 유니크 인덱스

- 중복을 허용하지 않은 인덱스
- 휴대폰 번호, 주민등록번호 등 고유값

## 8. 인덱스 생성, 조회, 삭제

```sql
-- 단일 컬럼 인덱스
CREATE INDEX IDX_CUSTOMER_NAME ON CUSTOMERS(NAME);

-- 결합 인덱스
CREATE INDEX IDEX_ORDER_CUSTOMER_DATE ON ORDERS(CUSTOMER_ID, ORDER_DATE);

-- 유니크 인덱스
CREATE UNIQUE INDEX IDX_EMAIL ON USERS(EMAIL);

-- 인덱스 확인
SELECT INDEX_NAME, TABLE_NAME, UNIQUENESS
FROM USER_INDEXES;

-- 인덱스 삭제
DROP INDEX INDEX_NAME

```

## 9. 인덱스 관리 팁

- 자주 사용되는 컬럼에 생성 : WHERE절, JOIN, ORDER BY, GROUP BY
- 적절한 인덱스 개수 유지
- 쓰기와 읽기 비율 고려
- 데이터 중복도 파악: 중복도가 낮은 컬럼은  B-Tree, 높은 컬럼은 Bitmap 인덱스 추천
- 인덱스 컬럼 순서
    - 결합 인덱스에서  SELECT 조건에 자주 사용되는 컬럼을 우선적으로 배치
- 카디널리티
    - 카디널리티(값의 고유도)가 높은 컬럼에 인덱스를 생성
    - 예 : 주민등록번호 > 성별

## 10. 인덱스를 활용하지 못하게 하는 주요 원인

- 인덱스 컬럼에 함수 또는 연산 사용
    
    ```sql
    -- 인덱스 컬럼 'SALARY'에 함수 적용
    SELECT * FROM EMPLOYEES WHERE ROUND(SALARY) = 5000;
    
    -- 해결 방법: 함수 사용 제거 
    SELECT * FROM EMPLOYEES WHERE SALARY BETWEEN 4999.5 AND 5000.5;
    ```
    
- 데이터 타입 불일치
    
    ```sql
    -- 인덱스 컬럼 'EMPLOYEE_ID'는 숫자 타입인데 문자로 비교
    SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = '101';
    
    -- 해결 방법: 데이터 타입 일치
    SELECT * FROM EMPLOYEES WHERE EMPLOYEE_ID = 101;
    ```
    
- 와일드카드(%) 사용 위치
    
    ```sql
    -- 인덱스가 사용되지 않음
    SELECT * FROM CUSTOMERS WHERE NAME LIKE '%John%';
    
    -- 해결 방법: 와일드카드 제거 또는 앞부분 고정
    SELECT * FROM CUSTOMERS WHERE NAME LIKE 'John%';
    ```
    
- NULL 값 처리
    
    ```sql
    -- 인덱스를 사용할 수 없음
    SELECT * FROM EMPLOYEES WHERE SALARY IS NULL;
    
    --해결 방법: NULL 값을 포함하는 별도 처리 필요
    ALTER TABLE EMPLOYEES MODIFY SALARY DEFAULT 0;
    ```
    
- OR 조건이 포함된 경우(or expansion 이 작동되면 인덱스를 탈 수 있다)
    
    ```sql
    -- 인덱스 활용 어려움
    SELECT * FROM EMPLOYEES WHERE DEPARTMENT_ID = 10 OR MANAGER_ID = 5;
    
    -- 해결 방법: UNION ALL로 분리
    SELECT * FROM EMPLOYEES WHERE DEPARTMENT_ID = 10
    UNION ALL
    SELECT * FROM EMPLOYEES WHERE MANAGER_ID = 5;
    ```
    

## 다양한 인덱스 종류

### REVERSE KEY INDEX

- 인덱스에 저장되는 키 값을 반전시켜 저장
- 숫자 12345 → 54321로 저장
- 인덱스에 시퀀스나 날짜 등

### CLUSTERED INDEX

- 테이블 데이터가 인덱스 순서에 따라 물리적으로 정렬되어 저장되는 인덱스

### IOT(INDEX ORGANIZED TABLE)

- 인덱스 자체에 모든 데이터가 저장
- 별도의 데이터와 인덱스를 유지하지 않으므로 저장 공간 절력
- PK 검색이 매우 빠르지만 그 외의 값으로 검색하는 경우에는 느림
