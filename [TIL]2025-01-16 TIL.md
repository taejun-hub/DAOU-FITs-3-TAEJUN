# SQL

## 1. JOIN

> 여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL절
> 

### INNER JOIN

- 두 테이블 간에 일치하는 데이터만 반환
- 기본적이 JOIN 방식
- 예시

```sql
-- 방식 1
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
    INNER JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
 
-- 방식 2
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### LEFT OUTER JOIN

- LEFT 테이블의 모든 행과 일치하는 RIGHT 테이블의 데이터를 반환
- 일치하지 않은 경우 NULL 반환
- 예시

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E 
LEFT OUTER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### FULL OUTER JOIN

- LEFT 테이블과 RIGHT 테이블의 모든 행 반환
- 일치하지 않은 데이터는 NULL로 표시
- 예시

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E 
    FULL OUTER JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### SELF JOIN

- 같은 테이블을 두 번 사용하여 JOIN 수행
- 주로 계층 구조를 표현할 때 사용
- 예시

```sql
SELECT E1.NAME EMPLOYEE, E2.NAME AS MANAGER
FROM EMPLOYEES E1 
    JOIN EMPLOYEES E2
        ON E1.EMPLOYEE_ID = E2.EMPLOYEE_ID
```

### CROSS JOIN

- 두 테이블 간의 모든 행의 조합을 반환(Cartesian Product)
- JOIN 조건이 없는 경우 발생
- 결과 집합의 크기가 매우 커질 수 있으므로 조심해서 사용
- 예시

```sql
SELECT E.NAME,D.DEPARTMENT_NAME
FROM EMPLOYEES E
    CROSS JOIN DEPARTMENTS D;
```

### NATURAL JOIN

- 동일한 이름의 컬럼을 기준으로 JOIN
- 조건을 명시하지 않아도 자동으로 연결
- 예시

```sql
SELECT NAME,DEPARTMENT_NAME
FROM EMPLOYEES
    NATURAL JOIN DEPARTMENTS;
```

### JOIN 사용 시 유의사항

- JOIN 조건 누락
- NULL 처리
    - OUTER JOIN시 NULL 값을 처리하는 로직 필요
- 성능
    - 큰 테이블 간 JOIN은 성능 저하 기능, 적절한 인텍스 설정 필요

### 예제

```sql
-- 각 지점의 이름과 대출 신청 고객의 이름과 대출 금액을 출력(대출 신청이 없는 지점도 포함)
SELECT B.NAME BRANCH_NAME, C.NAME CUSTOMER_NAME, L.AMOUNT
FROM BRANCHES B
    LEFT OUTER JOIN LOANS L
        ON B.BRANCH_ID = L.BRANCH_ID
    INNER JOIN CUSTOMERS C
        ON L.CUSTOMER_ID = C.CUSTOMER_ID;

-- 잔액이 50,000 이상인 계좌를 가진 고객의 이름과 이메일을 출력
SELECT C.NAME, C.EMAIL
FROM ACCOUNTS A
    INNER JOIN CUSTOMERS C
        ON A.CUSTOMER_ID = C.CUSTOMER_ID
WHERE A.BALANCE >= 50000;

-- 대출 상태가 PENDING인 대출을 신청한 고객 이름과 대출 금액을 조회
SELECT C.NAME, L.AMOUNT
FROM CUSTOMERS C
    INNER JOIN LOANS L
        ON C.CUSTOMER_ID = L.CUSTOMER_ID
WHERE L.STATUS = 'PENDING';
```

## 2.  SQL 실습

```sql
-- 1. 직원과 부서 정보 조회
-- 직원 이름과 소속 부서의 근무지를 조회
SELECT E.NAME, D.LOCATION
FROM EMPLOYEES E
LEFT JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;

-- 2. 직원과 매니저 정보 조회
-- 직원 이름과 해당 직원의 매니저 이름을 조회(단, 매니저가 없는 직원도 출력)
SELECT E1.NAME EMPLOYEE_NAME, E2.NAME MANAGER_NAME
FROM EMPLOYEES E1
LEFT JOIN EMPLOYEES E2
ON E1.MANAGER_ID = E2.EMPLOYEE_ID;
     
             
-- 3. 직원과 급여 정보 조회
-- 직원 이름과 급여를 조회하되 급여가 NULL인 경우 "N/A"로 출력T
SELECT NAME, NVL(TO_CHAR(SALARY), 'N/A') SALARY -- NVL() 사용 시 두 인수의 형식이 같아야 한다.
FROM EMPLOYEES;

--4. 특정 부서 직원 조회
--근무지가 Chicago인 직원 이름과 급여 조회
SELECT E.NAME, E.SALARY
FROM EMPLOYEES E
JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE D.LOCATION = 'Chicago';

-- 5. 
SELECT E.NAME, E.SALARY, D.DEPARTMENT_NAME
FROM EMPLOYEES E
JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
WHERE E.SALARY BETWEEN 5000 AND 10000;

--6. 최근 3개월 내 입사한 직원
-- 최근 3개월 이내 입사한 직원 이름과 입사일을 조회하세요.
SELECT NAME, HIRE_DATE
FROM EMLOYEES 
WHERE HIRE_DATE >= ADD_MONTH(SYSDATE, -3)
ORDER BY HIRE_DATE;

-- 7. 부서장 이름 조회
-- 각 부서의 부서장 이름과 부서 이름을 조회
SELECT E.NAME, D.DEPARTMENT_NAME
FROM DEPARTMENTS D
JOIN EMPLOYEES E
ON D.MANAGER_ID = E.EMPLOYEE_ID;

-- 8. 직무별 직원 수 조회
-- 직무별 직원 수 출력
SELECT JOB_ID, COUNT(*) EMPLOYEES
FROM EMPLOYEES
WHERE JOB_ID IS NOT NULL
GROUP BY JOB_ID;

-- 9. 부서별 평균 급여
-- 부서명과 부서별 평균 급여 조회(단, 평균 급여는 올림하여 출력)
SELECT D.DEPARTMENT_NAME, CEIL(AVG(E.SALARY)) AS AVG_SALARY
FROM EMPLOYEES E
JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID    
GROUP BY DEPARTMENT_NAME;

-- 10. 직원의 총 급여 합계
-- 직원의 총 급여 합계를 조회
SELECT SUM(SALARY) TOTAL_SALARY
FROM EMPLOYEES;

-- 11.
SELECT L.AMOUNT, C.NAME
FROM LOANS L
JOIN CUSTOMERS C
ON L.CUSTOMER_ID = C.CUSTOMER_ID
ORDER BY L.AMOUNT DESC;

--12. 대출 상태별 고객 수 조회
SELECT L.STATUS, COUNT(DISTINCT(CUSTOMER_ID)) -- 고객의 중복 제거
FROM LOANS L
GROUP BY L.STATUS;

--13. 직원 수가 가장 적은 부서의 부서 ID와 직원 수를 출력
SELECT DEPARTMENT_ID, COUNT(*) 
FROM EMPLOYEES 
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY DEPARTMENT_ID
ORDER BY COUNT(*)
FETCH FIRST 1 ROWS ONLY; -- ROWNUM과 서브쿼리를 통해 가능

-- 14. 월별 신규 고객 수를 최근순으로 조회
SELECT TRUNC(HIRE_DATE, 'MM'), COUNT(TRUNC(HIRE_DATE, 'MM'))
FROM EMPLOYEES
GROUP BY TRUNC(HIRE_DATE, 'MM')
ORDER BY TRUNC(HIRE_DATE, 'MM') DESC;

-- 15. 상품 카테고리와 상위 카테고리를 조회
SELECT C1.CATEGORY_NAME, C2.CATEGORY_NAME AS PARENT_CATEGORY_NAME
FROM SHOPPING_CATEGORIES C1
JOIN SHOPPING_CATEGORIES C2
ON C1.PARENT_CATEGORY_ID = C2.CATEGORY_ID;

-- 16. 특정 지점의 대출 합계
-- 'BRANCH 8' 지점의 대충 총합 조회(단, 소수점 아래는 버림)
SELECT B.NAME, FLOOR(SUM(AMOUNT))
FROM LOANS L
JOIN BRANCHES B
ON L.BRANCH_ID = B.BRANCH_ID
WHERE B.NAME = 'Branch 8'
GROUP BY B.NAME;

--17. 직무별 직원의 평균 급여와 급여 등급 조회
-- 직무별 직원의 평균 급여를 계산하여 
SELECT JOB_ID, AVG,
    CASE
        WHEN AVG >= 8000 THEN 'HIGH'
        WHEN AVG >= 6000 THEN 'MEDIUM'
        ELSE 'LOW'
    END AS GRADE
FROM (
    SELECT JOB_ID, AVG(SALARY) AS AVG
    FROM EMPLOYEES
    GROUP BY JOB_ID
)
WHERE AVG >= 5000
ORDER BY AVG DESC;

--18. 고객 이름과 거래 내역(금액)을 조회하여 고객 이름으로 오름차순
SELECT C.NAME, SUM(AMOUNT)
FROM CUSTOMERS C
JOIN ACCOUNTS A
ON C.CUSTOMER_ID = A.CUSTOMER_ID
JOIN TRANSACTIONS T
ON A.ACCOUNT_ID = T.ACCOUNT_ID
GROUP BY C.NAME
ORDER BY C.NAME DESC;
--ORDER BY TO_NUMBER(REGEXP_REPLACE(C.NAME,  '[^0-9]')); 숫자를 만 남기고 제거(숫자 기준 정렬))

-- 19. 부서별로 평균 근속일을 계산하여 부서이름으로 오름차순 정렬
SELECT D.DEPARTMENT_NAME, ROUND(AVG(TO_DATE(SYSDATE) - TO_DATE(HIRE_DATE))) AVG_DAYS_OF_WORK
FROM EMPLOYEES E
JOIN DEPARTMENTS D
ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
GROUP BY D.DEPARTMENT_NAME
ORDER BY D.DEPARTMENT_NAME;
--ORDER BY TO_NUMBER(REGEXP_REPLACE(D.DEPARTMENT_NAME,  '[^0-9]'));  숫자를 만 남기고 제거(숫자 기준 정렬))

-- 20. 대출 금액이 가장 높은 상위 5명의 고객의 이름과 대출 금액 조회(단, 대출 금액이 동일한 경우 고객 이름으로 오름차순)
SELECT * 
FROM (
    SELECT C.NAME CUSTOMER_NAME, SUM(L.AMOUNT) TOTAL_AMOUNT
    FROM CUSTOMERS C
    JOIN LOANS L
    ON C.CUSTOMER_ID = L.CUSTOMER_ID
    GROUP BY C.NAME
    ORDER BY TOTAL_AMOUNT DESC, C.NAME
)
WHERE ROWNUM  <= 5;

```
