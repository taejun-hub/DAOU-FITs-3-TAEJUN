# Java 객체 지향

## 1. instance

> class에 정의된 field와 method를 사용하기 위해서는 메모리에 일정 공간을 확보해야 한다. 이렇게 메모리 상에 생성된 일정 공간을 class의 instance, 혹은 객체(object)라고 한다.
> 

## 2. constructor(생성자)

- Java의 모든 class는 적어도 하나 이상의 constructor를 가진다.
- method와 비슷한 구조를 가지지만 method는 아니다. (리턴 타입이 존재하지 않는다.)
- constructor의 이름은 class의 이름과 같아야 하다.
- 객체가 생성될 때 `new`키워드와 함께 호출되며 객체가 가지는 필드에 대한 초기화 등의 작업

## 3. JVM의 메모리 구조

### Register

### Runtime Constant Pool

- 실행 시 상수(constant)를 저장하는 공간
- Heap영역의 일부

### Method Area

- 특정 클래스에 대한 정보가 담긴다.(클래스에 접근했을 때)
- static variable(class variable)이 저장
- 메서드에 대한 바이트 코드(실행 코드)가 저장
- 전체적으로 보면 Heap 영역에 속한다

### Heap

- 인스턴스가 저장

### Call Stack(Stack)

- 메서드 실행을 위한 메모리 공간

## 4. class member & instance menber

### class member

> 모든 instance(객체)가 `공유`하는 field와 method
> 
- static keyword로 선언
- 인스턴스가 안만들어져도 사용 가능
- class명 또는 instance명으로 access 가능

### instance member

> 각 instance가 독립적으로 메모리 공간을 확보하는 field와 method
> 
- instance를 이용해야 사용 가능

## 5. method overloading

### Overloading

> 같은 이름이나 같은 형태로 여러개의 기능을  수행하는 경우
> 

- 같은 이름의 method가 여러 개 존재하는 것을 의미
- method가 갖는 매개변수의 개수와 데이터 타입을 이용하여 이름은 같지만 서로 다른 method로 구분하는 개념
- 생성자에도 적용 가능(`Constructor Overloading`)

## 6. package & import

- source code를 효율적으로 관리하고 재사용하는 방법
- Java의 모든 class는 특정 package에 반드시 포함된다.(package를 지정하지 않으면 default package에 포함)

## 7. access modifier (접근제어자)

### public

- 어디에서나 접근 가능한 field, method, constructor, class 선언에 사용

### protected

- 기본적으로 default와 같지만 `상속관계`에 있는 class에서 field, method, constructor, class를 사용할 수 있도록 선언

### default

- 같은 `package내`에 다른 class에서 접근 가능한 field, method, constructor, class 선언

### private

- 다른 class에서는 접근할 수 없는 field, method, constructor 선언

<aside>
💡일반적으로 데이터는 private, method는 public으로 잡는다.

</aside>



```java
package bbb;

// class 앞에는 access modifier가 2가지중 하나만 올 수 있다.
// public or default
// public은 package에 상관없이 어디서나 access가 가능한 것을 의미.
// default는 같은 package 내에서만 access가 가능한 것을 의미.
public class MyClass {

    private int balance;

    // constructor
    public MyClass() {}

    // getter
    public int getBalance() {
        return this.balance;
    }
    // setter
    public void setBalance (int balance) {
        this.balance = balance;
    }

    public void printInfo() {}
}
```

## 8. static block

- static block은 class가 로드된 후 main thread에 의해서 main thread가 호출되기 이전에 실행
- 주로 JNI 같은 것을 이용할 때 사용

```java
public class StaticBlock {
    int k;

    // static block
    // 실행 시점: class에 대한 정보가 method area에 담긴 후 main()이 실행되기 전
    static {
        System.out.println("static block 호출");
    }

    public static void main(String[] args) {
        System.out.println("main() 호출");
    }
}

// 실행결과:
// static block 호출
// main() 호출
```

## 9. inheritance(상속)

- 상속은 부모 class가 가진 내용을 자식 class가 물려받는 것
- 부모 클래스(Parent class, `Super classs`,  Upper class, 상위 클래스)
- 자식 클래스(Child class, `Sub class`, Supper class, 하위 클래스)
- Java는 다중 상속을 지원하지 않음(`단일 상속`만 지원)
- class들은 결국 상속에 상속을 이어가면서 확장되어 만들고 사용된다(tree 구조)
- 자바의 모든 class는 상속관계에 있다 (`java.lang.Object` 클래스를 상속한다)
- 자식 클래스 선언 시 `extends` keyword를 이용하여 부모 클래스를 상속
- 상속이 항상 좋은 건 아니다

```java
// 상위 클래스
public class Person extends java.lang.Object {
    private String name;
    private String phone;
    private String address;

    public Person() {}

    // method
    public void sleep() {}

    public void eat() {}
}

// 하위 클래스
public class Teacher extends Person {

    private String subject;

    public Teacher() {}

    //method
    public void teach() {}
}

```
