# 포인터 응용

## 1. 포인터와 문자열

### 문자열 저장 방식

- **문자 배열**
- **문자열 포인터**

문자 배열을 포인터 변수에 저장하면 포인터 변수를 통해 문자 단위로 배열의 각 요소의 접근 가능하다

문자열 포인터로 직접 생성하면  **읽기 전용 메모리(상수 영역)** 에 저장되므로 **수정이 불가능**

```c
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <string.h>

void ptr_prac() {
	char city1[] = "seoul"; // 문자열 "seoul"은 stack영역에 저장
	char *city2 = "seoul"; // 문자열 "seoul"은 stack영역에 저장되고, 포인터 변수 city2는 stack 영역에 저장

	// 문자 배열 변경 방식이 다르다
	strcpy(city1, "busan");
	city2 = "busan";

	printf("\n%s", &city1);
	printf("\n%s", city2);

	char city3[] = "seoul";
	char city4[] = "jeju";

	char* pcity3 = city3;

	pcity3[0] = 'k';
	printf("\%s", pcity3);

}
```

## 2. 포인터 배열

- 포인터 자체를 배열로 만들어 사용
- 문자열 배열을 처리할 때 사용

```c
char city[3][8] = { "seoul", "jeju", "busan" }; // stack 영역에 저장
char *p_city[] = { "seoul", "jeju", "busan" }; // data 영역에 저장(가변 길이로 저장)
```

### **포인터 배열 vs 2차원 배열 차이점**

| 특징 | 포인터 배열 `char *arr[]` | 2차원 배열 `char arr[][]` |
| --- | --- | --- |
| 저장 위치 | **문자열 리터럴 (.rodata)** | **스택/데이터 영역** |
| 수정 가능 여부 | ❌ 불가능 (`arr[0][0] = 'A';` 오류) | ✅ 가능 (`arr[0][0] = 'A';`) |
| 메모리 할당 | 문자열 크기가 가변적 | 고정 크기 할당 |
| 크기 변경 | ✅ 가능 (재할당 가능) | ❌ 불가능 (고정 크기) |

## 3. 함수 포인터

- 함수의 시작주소를 변수에 저장하여 사용
- 프로그램은 일반적으로 컴파일러에 의해 함수 단위의 실행 파일로 만들어진다
- 함수 포인터는 같은 형태의 함수가 여러 개 있을 때 하나의 함수 포인터로 여러 개의 함수를 호출하여 사용할 경우 유용하다

### 함수 포인터 선언 형식

```c
// 반환형 (*포인터이름)(매개변수 타입 ...);
int (*fp)(int, int);  // int 타입을 반환하고, int 두 개를 매개변수로 받는 함수 포인터
```

### 함수 포인터 배열

```c
#include <stdio.h>

int add(int a, int b) { return a + b; }
int subtract(int a, int b) { return a - b; }
int multiply(int a, int b) { return a * b; }
int divide(int a, int b) { return b != 0 ? a / b : 0; }

int main() {
    int (*operations[4])(int, int) = { add, subtract, multiply, divide };

    int x = 10, y = 5;
    printf("Addition: %d\n", operations[0](x, y));
    printf("Subtraction: %d\n", operations[1](x, y));
    printf("Multiplication: %d\n", operations[2](x, y));
    printf("Division: %d\n", operations[3](x, y));

    return 0;
}
```

### 함수 포인터를 매개변수로 전달

```c
#include <stdio.h>

// 연산 함수
int add(int a, int b) { return a + b; }
int multiply(int a, int b) { return a * b; }

// 연산을 수행하는 함수 (함수 포인터를 매개변수로 받음)
int calculate(int x, int y, int (*operation)(int, int)) {
    return operation(x, y); // 콜백 실행
}

int main() {
    printf("Addition: %d\n", calculate(10, 5, add));      // add 함수 전달
    printf("Multiplication: %d\n", calculate(10, 5, multiply));  // multiply 함수 전달
    return 0;
}
```

### `typedef` 를 이용한 함수 포인터

- 함수 포인터 선언이 길어질 경우 `typedef`를 사용하면 코드가 더 깔끔해진다

```c
#include <stdio.h>

// 함수 포인터 타입 정의
typedef int (*Operation)(int, int);

int max(int x, int y) {
	return x > y ? x : y;
}

int min(int x, int y) {
	return x < y ? x : y;
}

void prob_27() {
	Operation op;
	int num1, num2, flag;

	printf("\n두 개의 숫자를 입력 : ");
	scanf_s("%d %d", &num1, &num2);

	printf("\n원하는 값을 입력(1: 큰값, 2: 작은값): ");
	scanf_s("%d", &flag);

	if (flag == 1)
	{
		op = max;
		printf("\n두 수중 큰 값은: %d", op(num1, num2));
	}
	else if (flag == 2)
	{
		op = min;
		printf("\n두 수중 작은 값은: %d", op(num1, num2));
	}
	else
	{
		printf("\n잘못된 숫자 입력입니다.");
	}

}
```
