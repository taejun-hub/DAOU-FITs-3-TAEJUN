# C 언어 연산자와 제어문 - TIL (Today I Learned)

## 1. 연산자 (Operators)

C 언어에서 연산자는 변수나 값에 대해 연산을 수행하는 기호이다.

### 1.1 산술 연산자 (Arithmetic Operators)

| 연산자 | 설명 | 예제 |
| --- | --- | --- |
| `+` | 덧셈 | `a + b` |
| `-` | 뺄셈 | `a - b` |
| `*` | 곱셈 | `a * b` |
| `/` | 나눗셈 | `a / b` (정수 나눗셈 주의) |
| `%` | 나머지 | `a % b` |

### 1.2 대입 연산자 (Assignment Operators)

| 연산자 | 설명 | 예제 |
| --- | --- | --- |
| `=` | 값 대입 | `a = b` |
| `+=` | 덧셈 후 대입 | `a += b` (→ `a = a + b`) |
| `-=` | 뺄셈 후 대입 | `a -= b` |
| `*=` | 곱셈 후 대입 | `a *= b` |
| `/=` | 나눗셈 후 대입 | `a /= b` |
| `%=` | 나머지 후 대입 | `a %= b` |

### 1.3 비교 연산자 (Comparison Operators)

| 연산자 | 설명 | 예제 |
| --- | --- | --- |
| `==` | 같음 | `a == b` |
| `!=` | 다름 | `a != b` |
| `>` | 초과 | `a > b` |
| `<` | 미만 | `a < b` |
| `>=` | 이상 | `a >= b` |
| `<=` | 이하 | `a <= b` |

### 1.4 논리 연산자 (Logical Operators)

| 연산자 | 설명 | 예제 |
| --- | --- | --- |
| `&&` | AND (논리곱) | `(a > 0) && (b > 0)` |
| `||` | OR (논리합) |  |
| `!` | NOT (부정) | `!(a > 0)` |

### 1.5 비트 연산자 (Bitwise Operators)

| 연산자 | 설명 | 예제 |
| --- | --- | --- |
| `&` | 비트 AND | `a & b` |
| `|`  | 비트 OR | `a | b` |
| `^` | 비트 XOR | `a ^ b` |
| `~` | 비트 NOT | `~a` |
| `<<` | 왼쪽 시프트 | `a << 2` |
| `>>` | 오른쪽 시프트 | `a >> 2` |
- 예제

```c
//16비트의 부호 없는 정수형 데이터를 오른쪽으로 1비트 이동하면 오른쪽에 있는 비트가 
//제거됩니다. 이때 제거되는 비트를 왼쪽의 최상위 비트에 채워 넣은 오른족 로테이션(right 
//rotation)프로그램을 작성
void prob_1() {
	int a;
	printf("\n정수형 데이터 입력 : ");
	scanf_s("%d", &a);

	printf("로테이트 전 %d : ", a);
	for (int i = 15; i >= 0; i--) {
		a & (1 << i) ? printf("%c", '1') : printf("%c", '0');
	}
	int temp = a;
	a = a >> 1;
	a = temp & 1 ? a | (1 << 15) : a;

	printf("\n로테이트 후 %d : ", a);
	for (int i = 15; i >= 0; i--) {
		a & (1 << i) ? printf("%c", '1') : printf("%c", '0');
	}

}
```

### 1.6 삼항 연산자 (Ternary Operator)

```
result = (a > b) ? a : b;  // a가 b보다 크면 a, 아니면 b 반환
```

---

## 2. 제어문 (Control Statements)

C 언어의 제어문은 프로그램의 흐름을 제어하는 역할을 한다.

### 2.1 조건문 (Conditional Statements)

### if-else if 문

```c
if (a > 0) {
    printf("양수");
} else if (a < 0) {
    printf("음수");
} else {
    printf("0");
}
```

### switch 문

```c
switch (value) {
    case 1:
        printf("1입니다");
        break;
    case 2:
        printf("2입니다");
        break;
    default:
        printf("그 외 값입니다");
}
```

---

### 2.2 반복문 (Loop Statements)

### do-while 문

```c
int i = 0;
do {
    printf("%d\n", i);
    i++;
} while (i < 5);
```

---

### 2.3 반복문 제어 (Loop Control)

### break 문

```c
for (int i = 0; i < 10; i++) {
    if (i == 5) break;  // 5에서 반복 종료
    printf("%d\n", i);
}
```

### continue 문

```c
for (int i = 0; i < 10; i++) {
    if (i % 2 == 0) continue;  // 짝수 건너뛰기
    printf("%d\n", i);
}
```

---
