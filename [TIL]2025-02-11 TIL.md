# **JavaFX**

JDBC Program을 하기 위해 `JavaFX`를 이용한다. 비록 JavaFX가 실무에서는 그다지 많이 사용되지는 않지만 상당히 편한 interface를 제공해 주고 특히 JDBC 프로그램에서 `MVC Pattern`을 공부하기에 좋은 구조를 가지고 있다.

## **1. JavaFX란**

> JAVA GUI 프로그램 개발을 위한 라이브러리
> 
- Swing을 대체하기 위해 만들어진 GUI 라이브러리
- 하드웨어 가속 지원
- 크로스 플랫폼 지원
- FXML - XML기반의 마크업 언어로 작성하는 UI
- CSS를 이용해 디자인 가능
- JDK 8 까지는 기본 제공 라이브러리였으나 JDK 11부터 분리됨

## **2. JavaFX 설치**

다음의 링크에 접속합니다.

[JavaFX - Gluon](https://gluonhq.com/products/javafx/)

Java FX는 기본적으로 `Java 11` 이상을 지원합니다. 가능한 최신버전을 사용하길 권장하니 22버전 아니면 17.0 LTS버전을 사용하는게 좋을 듯 합니다. `SDK` 타입 다운로드를 클릭합니다.

다운로드 후 편의를 위해 Java 설치폴더로 복사한 후 압축을 풀어줍니다. (사실 어디에서 압축을 풀어도 상관없습니다.)

## 3 **JavaFX 설정**

- `File` > `Project Structure` > `Libraries`로 이동합니다.
- JavaFX SDK의 `lib` 폴더를 추가합니다.

`VM arguments`  지정

```java
--module-path "C:\Program Files\Java\openjfx-17.0.14_windows-x64_bin-sdk\javafx-sdk-17.0.14\lib" --add-modules javafx.controls,javafx.fxml
```

## 4. 간단한 코드 구현

### 화면 구성 방법

- layout manager 만들고
- component 를 layout manager 에 추가
- layout manager를 scene 에 붙이고
- scene 을 stage에 붙이는 작업을 수행

### 이벤트 처리(Event Delegation Model)

- event source(이벤트 발생 객체)
- event 객체 (event 세부 정보를 가지고 있음)
- event 처리 객체 (event handler)

```java
package fxtest;

import javafx.application.Application;
import javafx.event.ActionEvent;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.layout.StackPane;
import javafx.stage.Stage;

import java.util.concurrent.atomic.AtomicInteger;

public class Main extends Application {

    @Override
    public void start(Stage stage) throws Exception {
    // 화면 구성과 이벤트 처리에 대한 코드

        // button component 생성
        Button btn = new Button();
        btn.setText("click me");
        AtomicInteger cnt = new AtomicInteger();

        // event handler
        // - action event : 마우스 클릭, 엔터
        btn.setOnAction((ActionEvent event) -> System.out.println("click count: " + cnt.addAndGet(1)));

        // layout manager 생성
        StackPane root = new StackPane();

        // 버튼을 layout manager 에 부착
        root.getChildren().add(btn);

        // layout manager 를 이용하여 scene 생성
        Scene scene = new Scene(root, 300, 200);

        // stage 에 scene 을 세팅
        stage.setScene(scene);

        stage.setTitle("Hello World");
        stage.show();
    }
    public static void main(String[] args) {
        launch(args);
    }
}

```

## 5. Scene Builder

> JavaFX에서 화면 구성을 쉽게 할 수 있도록 도와주는 시각적 레이아웃 도구이며, `FXML` 파일을 생성해준다.
> 

### 사용법

- scene builder 설치

[Scene Builder - Gluon](https://gluonhq.com/products/scene-builder/)

- scene builder를 실행하여 화면 구성 후 fxml 파일 생성
- fxml 파일을 load 후에 scene에 등록

### Controller

> 이벤트 처리를 위한 Controller Class
> 
- `Initializable` 인터페이스를 반드시 구현해야 한다
- `initialize()` 메소드 내에서 이벤트 등록을 해준다

```java
package javaFXsimple;

import javafx.fxml.FXML;
import javafx.fxml.Initializable;
import javafx.scene.control.Button;

import java.net.URL;
import java.util.ResourceBundle;

public class MyStageController implements Initializable {
    @FXML private Button firstBtn;
    @FXML private Button secondBtn;

    @Override
    public void initialize(URL url, ResourceBundle resourceBundle) {
        // 이벤트 처리 코드
        System.out.println("Controller initialized");
        firstBtn.setOnAction(e -> System.out.println("First button clicked"));
        secondBtn.setOnAction(e -> System.out.println("Second button clicked"));
    }
}
```

```java
import javafx.stage.Stage;

import java.io.IOException;

public class MyStage extends Stage {

    public MyStage() {
        // 여기는 Scene Builder 를 이용하여 화면 구성
        Parent root = null;
        FXMLLoader fxmlLoader = new FXMLLoader(getClass().getResource("example.fxml"));

        try {
            root = fxmlLoader.load();
            Scene scene = new Scene(root);
            this.setScene(scene);
            this.setTitle("My Stage");
        } catch (IOException e) {
            System.out.println("Error in loading fxml");
        }
    }
}
```

```java
<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.Button?>
<?import javafx.scene.layout.FlowPane?>
<?import javafx.scene.text.Font?>

<FlowPane **fx:controller="javaFXsimple.MyStageController"** maxHeight="-Infinity" maxWidth="-Infinity" minHeight="-Infinity" minWidth="-Infinity" prefHeight="400.0" prefWidth="600.0" xmlns="http://javafx.com/javafx/23.0.1" xmlns:fx="http://javafx.com/fxml/1">
   <children>
      <Button **fx:id="firstBtn"** mnemonicParsing="false" text="Button 1" textFill="#b01b1b">
         <FlowPane.margin>
            <Insets left="50.0" top="100.0" />
         </FlowPane.margin>
         <font>
            <Font size="24.0" />
         </font>
      </Button>
      <Button **fx:id="secondBtn"** mnemonicParsing="false" text="Button 2">
         <font>
            <Font size="24.0" />
         </font>
         <FlowPane.margin>
            <Insets left="30.0" top="100.0" />
         </FlowPane.margin>
      </Button>
   </children>
</FlowPane>

```

---

# VO, DO, Entity, DTO

## 1. DO (Domain Object)

> Domain에서 사용하는 데이터를 객체화 시킨 것
> 

## 2. VO (Value Object)

> 각 table의 하나의 row를 담을 수 있는 객체
> 
- 도메인 객체(`DO`)의 일종이고 보통 기본키로 식별 값을 갖는 `Entity`와 구별해서 사용

## 3. Entity

- VO와 유사하지만 `Primary key`를 포함

## 4. DTO (Data Transfer Object)

- VO 와 똑같지만 데이터 전송하는 사용 방식에서 DTO라 부른다

# Layered Architecture (4계층)

### Presentation Layer

- 입출력 처리
- 주요 구성요소는 View 와 Controller

### Business(Logic) Layer

- 로직 처리
- 주요 구성요소는 Service 와 Domain Model

### Persistence Layer

- DB 접근
- 대표적인 구성요소는 Repository, DAO 등이 있다

### Database Layer

- oracle, mysql 등 데이터베이스가 위치한 계층

# MVC Pattern

> 모델-뷰-컨트롤러(model-view-controller)는 소프트웨어 공학에서 사용되는 소프트웨어 디자인 패턴
> 

## 1. Model

### Domain Model

- 비지니스 로직에서 처리하는 데이터를 의미
- 도메인 모델 역할을 하는 클래스를 Domain Object, VO, DTO 라고 한다

### Service Model

- 도메인 객체를 이용하여 비지니스 로직을 처리
- DB처리는 `DAO`(Data Access Object) 로 분리하기도 한다.

## 2. View

- 입출력 담당(Presantation Layer)
- 유저 인터페이스

## 3. Controller

- Model과 View 사이를 이어주는 브릿지 역할을 수행
- 사용자가 어플리케이션을 조작하여 발생하는 변경 이벤트들을 처리하는 역할
