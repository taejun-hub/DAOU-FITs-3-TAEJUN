# 비트 필드(Bit Fields)

> 비트 필드는 구조체에서 개별 비트를 사용하여 메모리를 절약하는 기술
> 

## 1. 비트 필드 특징

- 일반적으로 부울 값(0 또는 1)이나 작은 범위의 정수를 저장할 때 사용
- `int`, `unsigned int`, `char` 의 **기본 데이터 타입에 비트 개수를 지정**하여 사용
- 최적화된 메모리 관리가 필요할 때 유용(하드웨어 레지스터, 프로토콜 헤더 등)
- 비트 멤버를 생략하면 그 비트 수만큼 버린다
- 비트 필드는 하드웨어 종속적이며, 크기와 정렬 방식이 컴파일러마다 다를 수 있음
- 비트 필드는 기본 데이터 타입을 기준으로 할당됨.
- 비트 필드는 **컴파일러의 구현 방식에 따라 내부적으로 정렬(Padding)이 포함될 수도 있음**.

## 2. 비트 필드 사용법

```c
#include <stdio.h>

// 비트 필드 구조체 정의
typedef struct {
    unsigned int is_active: 1;          // 활성화 여부 => 1비트 (0 또는 1)
    unsigned int is_vip : 1;            // VIP 여부 => 1비트 (0 또는 1)
    unsigned int has_loan : 1;          // 대출 여부 => 1비트 (0 또는 1)
    unsigned int over_protection : 1;   // 예금 보호 한도 초과 여부 => 1비트
    unsigned int balance_status : 3;    // 잔액 상태 => 3비트 (예약된 필드)
} AccountStatus;

void printAccountStatus(AccountStatus account) {
    printf("계좌 상태:\n");
    printf(" - 활성화: %s\n", account.is_active ? "예" : "아니오");
    printf(" - VIP: %s\n", account.is_vip? "예" : "아니오");
    printf(" - 대출 여부: %s\n", account.has_loan? "예" : "아니오");
    printf(" - 예금 보호 한도 초과: %s\n", account.over_protection ? "예" : "아니오");
    printf(" - 활성화: %s\n", account.is_active ? "예" : "아니오");

    const char* balance_labels[] = { "0원", "1~10만 원", "10~50만 원", "50~100만 원",
        "100~500만 원", "500~1000만 원", "1000~5000만 원", "5000만 원 이상"
    };
    printf(" - 잔액 상태: %s\n", balance_labels[account.balance_status]);
}

int main() {
    AccountStatus customer1 = { 1, 1, 1, 1, 5 };

    printAccountStatus(customer1);
    printf("AccountStatus 구조체 크기: %lu 바이트\n", sizeof(AccountStatus));
		// 4바이트 인 이유 => 비트 필드가 CPU의 기본 워드 크기(예: 32비트)에 맞게 정렬 
    return 0;
}
```

## **3. 비트 필드의 장점과 단점**

### **장점**

- **메모리 절약** → 불필요한 메모리 낭비 없이 필요한 만큼만 사용 가능
- **가독성 증가** → 플래그 값을 의미 있는 변수명으로 표현 가능
- **하드웨어 제어에 유용** → 특정 비트를 제어하는 레지스터 관리에 활용 가능

### **단점**

- **이식성 문제** → 컴파일러 및 시스템에 따라 메모리 정렬 방식이 달라질 수 있음
- **퍼포먼스 문제** → 비트 연산이 필요하므로 단순 정수보다 성능이 떨어질 수 있음
- **연산 제약** → 비트 필드에는 `sizeof()`를 사용할 수 없음 (`sizeof(struct Status)`는 가능하지만 `sizeof(struct Status.mode)`는 불가능)

# 비트 마스크

> 비트 마스크란 비트 연산을 이용하여 특정 비트만 설정하거나 확인하는 기법
> 

## 비트 마스크를 통한 상태 플래그 저장

```c
#define READ_MASK 0x01
#define WRITE_MASK 0x02
#define EXCUTE_MASK 0x04
#define ADMIN_MASK 0x08

typedef struct {
    uint8_t flags;
} AuthStatus;

void bit_prob2() {
    int temp;
    AuthStatus authStatus;
    printf("사용자 권한을 설정합니다.\n");
    printf("READ 권한을 부여할까요? (1: 예, 0: 아니오): ");
    scanf_s("%d", &temp);
    authStatus.flags |= temp ? READ_MASK : 0;
    printf("WRITE 권한을 부여할까요? (1: 예, 0: 아니오): ");
    scanf_s("%d", &temp);
    authStatus.flags |= temp ? WRITE_MASK : 0;
    printf("EXCUTE 권한을 부여할까요? (1: 예, 0: 아니오): ");
    scanf_s("%d", &temp);
    authStatus.flags |= temp ? EXCUTE_MASK : 0;
    printf("ADMIN 권한을 부여할까요? (1: 예, 0: 아니오): ");
    scanf_s("%d", &temp);
    authStatus.flags |= temp ? ADMIN_MASK : 0;

    printf("\n사용자 권한 상태:\n");
    printf(" - READ: %s\n", authStatus.flags & READ_MASK ? "O" : "X");
    printf(" - WRITE: %s\n", authStatus.flags & WRITE_MASK ? "O" : "X");
    printf(" - EXCUTE: %s\n", authStatus.flags & EXCUTE_MASK ? "O" : "X");
    printf(" - ADMIN: %s\n", authStatus.flags & ADMIN_MASK ? "O" : "X");

    printf("UserPermissions 구조체 크기: %lu 바이트\n", sizeof(AuthStatus));

}
```

# 파일

## **1. 파일 입출력 기본 개념**

파일 입출력에서는 **파일을 열고, 읽거나 쓰고, 닫는 과정이 필수적**입니다.

### **파일 입출력 기본 흐름**

1️⃣ **파일 포인터(`FILE *`) 선언**

2️⃣ **`fopen()`을 사용하여 파일 열기**

3️⃣ **`fscanf()` 또는 `fprintf()`를 사용하여 파일 읽기/쓰기**

4️⃣ **`fclose()`를 사용하여 파일 닫기**

---

## **2. 파일 열기 (`fopen()`)**

파일을 열 때는 `fopen()` 함수를 사용하며, 모드를 설정해야 합니다.

### **파일 열기 모드**

| 모드 | 설명 | 파일이 없으면 |
| --- | --- | --- |
| `"r"` | 읽기 모드 (파일이 존재해야 함) | 오류 |
| `"w"` | 쓰기 모드 (파일이 없으면 생성, 있으면 덮어씀) | 생성 |
| `"a"` | 추가 모드 (파일이 없으면 생성, 있으면 데이터 추가) | 생성 |
| `"r+"` | 읽기 및 쓰기 모드 (파일이 존재해야 함) | 에러 |
| `"w+"` | 읽기 및 쓰기 모드 (파일이 없으면 생성, 있으면 덮어씀) | 생성 |
| `"a+"` | 읽기 및 추가 모드 (파일이 없으면 생성, 있으면 데이터 추가) | 생성 |

### **파일 열기 예제**

```c

#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "w");  // "data.txt"를 쓰기 모드로 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다!\n");
        return 1;
    }

    printf("파일이 성공적으로 열렸습니다.\n");

    fclose(file);  // 파일 닫기
    return 0;
}
```

- **파일을 열 때는 항상 `NULL` 체크를 해야 함!**
- **파일이 열리지 않으면 권한 문제, 파일 존재 여부 등을 확인**

---

## **3. 파일 쓰기 (`fprintf()`, `fputs()`, `fputc()`)**

### **`fprintf()`: 서식을 지정하여 문자열 쓰기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "w");  // 쓰기 모드로 파일 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다!\n");
        return 1;
    }

    fprintf(file, "이것은 C 파일 입출력 예제입니다.\n");
    fprintf(file, "숫자: %d, 실수: %.2f\n", 42, 3.14);

    fclose(file);
    return 0;
}
```

### **`fputs()`: 문자열 쓰기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "w");
    if (file == NULL) return 1;

    fputs("Hello, World!\n", file);  //파일에 문자열 저장
    fclose(file);
    return 0;
}
```

---

### **`fputc()`: 한 글자 쓰기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "w");
    if (file == NULL) return 1;

    fputc('A', file);  // 파일에 'A' 저장
    fclose(file);
    return 0;
}
```

---

## **4. 파일 읽기 (`fscanf()`, `fgets()`, `fgetc()`)**

### **`fscanf()`: 형식 지정하여 데이터 읽기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "r");  // 읽기 모드로 파일 열기
    if (file == NULL) {
        printf("파일을 열 수 없습니다!\n");
        return 1;
    }

    char str[100];
    int num;
    float fnum;

    fscanf(file, "%s %d %f", str, &num, &fnum);
    printf("읽은 데이터: %s %d %.2f\n", str, num, fnum);

    fclose(file);
    return 0;
}
```

---

### **`fgets()`: 한 줄씩 문자열 읽기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "r");
    if (file == NULL) return 1;

    char buffer[100];
    while (fgets(buffer, sizeof(buffer), file) != NULL) {
        printf("%s", buffer);
    }

    fclose(file);
    return 0;
}
```

---

### **`fgetc()`: 한 글자씩 읽기**

```c
#include <stdio.h>

int main() {
    FILE *file = fopen("data.txt", "r");
    if (file == NULL) return 1;

    char ch;
    while ((ch = fgetc(file)) != EOF) {
        putchar(ch);
    }

    fclose(file);
    return 0;
}
```
