# 정규화(Normalization)

> 데이터베이스에서 설계 단계에서 데이터 중복을 제거하고 데이터 무결성을 유지하기 위한 과정
> 

## 제1정규형(1NF)

- 모든 속성은 반드시 하나의 값만 가져야 한다.
- 유사한 속성이 반복되는 경우도 1차 정규화의 대상

## 제2정규형(2NF)

- 엔티티의 모든 일반 속성은 반드시 모든 주식별자에 종속되어야 한다.

## 제3정규형(3NF)

- 주식별자가 아닌 모든 속성 간에는 서로 종속될 수 없다.

## 반정규화

> 데이터베이스 설계에서 정규화를 통해 분리된 테이블을 특정 성능 향상이나 시스템 요구에 따라 다시 결합하거나 중복 데이터를 허용하는 과정
> 

### 필요성

- **정규화의 한계**
    - 정규화를 통해 데이터 무결성과 중복 문제를 해결하지만, 다수의 테이블 조인이 필요한 쿼리에서는 성능 저하 발생 가능
    - 읽기 속도가 중요한 상황에서 정규화된 데이터 모델은 비효율적일 수 있음
- **성능 최적화**
    - 쿼리 성능을 개선하기 위해 데이터를 중복 저장하거나 테이블을 합치는 방식으로 읽기 속도를 높임
    - 주로 OLAP(Online Analytical Processing) 시스템에서 활용

### 적용방법

- 속성 추가
    - 예시: ORDERS 테이블에 CUSTOMER_NAME 속성을 추가(조인을 생략하고 직접 데이터를 조회 가능)
- 테이블 병합
    - 예시 PRODUCTS와 PRODUCT_DETAILS 두 테이블을 병합하여 하나의 테이블로 사용
- 중복 데이터 저장
    - 데이터 분석 속도를 위해 요약 데이터를 미리 계산하고 테이블에 저장
- 이력 데이터 관리
    - 트랜잭션 데이터 외에도 과거 데이터를 효율적으로 조회하기 위해 반정규화 적용

### 장점

- 성능 개선
    - 읽기 속도 증가, 조인 연산 감소
    - 대규모 데이터 분석과 보고서 생성 시 유리
- 간단한 쿼리
    - 테이블 구조 단순화로 인해 SQL 쿼리가 간결해짐
    - 사용자가 테이블 구조를 쉽게 이해할 수 있슴
- 특정 요구사항 대응
    - 응용 프로그램에서 특정 데이터 집합을 자주 조회해야 하는 경우 적합

### 단점

- 데이터 중복
    - 저장 공간 증가
    - 데이터 변경 시 모든 중복 데이터를 동기화해야 하는 복잡성 증가
- 데이터 무결성 문제
    - 업데이트나 삭제 시 데이터 불일치 가능성이 높아짐
- 유연성 감소

# OLTP vs OLAP

## 1. OLTP(Online Transaction Processing)

> 실시간으로 다수의 트랜잭션을 처리하는 시스템
> 
- 주로 운영 환경에서 사용
- 주요 목적: 데이터 입력, 저장, 수정 및 조회를 빠르게 처리
- 예시: 은행 시스템, 전자상거래, 예약 시스템

### 주요 특징

- 실시간 처리
- 정규화된 데이터 구조
- 트랜잭션 관리: ACID 속성을 보장

### 구성 요소

- 데이터베이스: RDBMS(Oracle, MySQL 등)
- 애플리케이션
- 사용자

## 2. OLAP(Online Analytical Processing)

> 대규모 데이터를 분석하고, 요약 정보와 통찰력을 제공하는 시스템
> 
- 주요 목적: 데이터의 분석, 보고서 생성, 비지니스 인사이트 생성
- 예시: 데이터 마트, 비지니스 인텔리전스, 대시보드 시스템

### 주요 특징

- 복잡한 분석
- 대규모 데이터 처리
- 다차원 데이터 분석

### 구성 요소

- 데이터 웨어하우스: 정형, 비정형 데이터를 통합하고 분석 중심으로 저장
- OLAP 서버: 분석 쿼리를 실행하며 다차원 데이터 모델 제공
- 사용자 인터페이스

## 3. OLTP와 OLAP의 상호작용

- OLTP 시스템은 테이터 생성, OLAP 시스템은 데이터 활용
- ETL 프로세스: 데이터 웨어하우스로 데이터를 전송하기 위해 ETL(Extract, Transform, Load)사용
- OLTP → ETL → OLAP 흐름

# 논리 모델 vs 물리 모델

## 1. 논리 모델

> 데이터베이스 설계의 한 단계로 데이터의 구조와 관계를 기술하는 모델
> 
- 엔티티와 속성 정의
- 관계와 제약 조건 표현

## 2. 물리 모델

> 데이터베이스 설계의 최종 단계로 특정 DBMS에 종속적인 데이터 구조를 정의하는 모델
> 
- 데이터 타입과 제약
- 인덱스, 파티션, 저장소 구조 설계

## 3. 논리 모델 → 물리 모델

- 변환 시 고려사항
    - 데이터베이스 크기와 예상 트래픽
    - 읽기 및 스기 작업의 비율
    - DBMS의 기능 및 제약

# SQL(Structured Query Language)

## 1. SQL이란?

- 데이터베이스를 관리하고 조작하기 위한 언어
- 관계형 데이터베이스에서 데이터를 CRUD하는 데 사용
- 데이터 정의, 조작, 제어를 위한 명령어 집합 제공

## 2. SQL 분류

### DDL

> 데이터베이스 객체를 정의하거나 변경하는 데 사용
> 
- 주요 명령어
    - CREATE: 객체(테이블, 뷰, 인덱스) 생성
    - ALTER: 기존 객체 수정
    - DROP: 객체 삭제
    - TRUNCATE: 테이블 데이터 초기화

### DML

> 데이터베이스의 데이터를 조작(삽입, 수정, 삭제)하는 데 사용
> 
- 주요 명령어
    - SELECT: 데이터 검색
    - INSERT: 데이터 삽입
    - UPDATE: 데이터 수정
    - DELETE: 데이터 삭제

### DCL

> 데이터 접근 권한 제어
> 
- 주요 명령어
    - GRANT: 권한 부여
    - REVOKE: 권한 회수

### TCL

- 트랜잭션 관리를 위한 명령어 제공
- 주요 명령어
    - COMMIT: 트랜잭션 확정
    - ROLLBACK: 트랜잭션 취소
    - SAVEPOINT: 특정 지점 저장

## 3. SQL 기초

### SELECT

- SQL에서 데이터를 검색하는 명령어
- 테이블에서 원하는 데이터를 추출
- 조건, 정렬, 그룹핑 등을 사용하여 복잡한 질의 가능
- 기본 문법

```sql
SELECT 칼럼명 FROM 테이블명;
```

### ALIAS

> SQL 쿼리에서 컬럼이나 테이블에 부여하는 임시 이름
> 
- 테이블이나 컬럼 이름 대신 사용할 수 있음
- 데이터베이스 구조에 영향을 주지 않는 임시 이름
- 컬럼 ALIAS의 경우 AS 키워드를 사용하지만 생략도 가능

- **컬럼 ALIAS**
    - 사용 이유
        - 연산 결과에 의미를 부여
        - 출력 결과의 컬럼 이름을 변경하여 가독성을 높임
    - 기본 문법
    
    ```sql
    SELECT 컬럼명 (AS) 별칭 FROM 테이블명;
    ```
    

- **테이블 ALIAS**
    - 사용 이유
        - 테이블 이름이 길거나 복잡할 때 간결한 이름으로 대체
        - JOIN 쿼리에서 테이블 이름을 축약하여 사용
    - 기본 문법
    
    ```sql
    SELECT 컬럼명 FROM 테이블명 별칭;
    ```
    

- 활용
    - 조합사용
    
    ```sql
    SELECT E.NAME AS EMPLOY_NAME,
    	D.DEPARTMENT_NAME AS DEPT_NAME
    FROM employees E, departments D
    WHERE e.department_id = d.department_id;
    
    ```
    
    - 특수문자 또는 공백, 한글 포함
    
    ```sql
    SELECT NAME AS "이름", SALARY AS "급여"
        FROM employees E;
    ```
    

### NULL

- 데이터베이스에서 NULL은 “값이 없음”을 의미
- 어떤 데이터도 저장되지 않은 상태
- 데이터가 “비어 있다”고 간주되며, 이미 0이나 빈 문자열(””)과는 다름
- NULL 값은 다른 값과 비교할 수 없음(비교 연산을 수행하면 FALSE를 반환)
- 예시

```sql
SELECT * FROM employees WHERE salary = NULL;
```

### WHERE

- 조건을 지정하여 데이터를 필터링
- SELECT, UPDATE, DELETE와 함께 사용
- 다양한 연산자를 활용하여 복잡한 조건 설정 가능
    - 비교 연산자: =, <>, >, <, >=
    - 논리 연산자: AND, OR, NOT
    - 특수 연산자: LIKE, IN, BETWEEN, IS NULL

### 특수 연산자 - LIKE

- 특정 패턴을 가진 데이터 검색
- %: 0개 이상의 문자를 대체
- _: 1개의 문자를 대체
- 예시
    
    ```sql
    SELECT * FROM CUSTOMERS WHERE NAME LIKE 'C%';
    ```
    

- **이스케이프**

```sql
-- 기본 문법
SELECT 컬럼명 FROM 테이블명 WHERE 컬럼명 LIKE '패턴' ESCAPE '문자'

-- 예시
SELECT NAME FROM CUSTOMERS
WHERE NAME LIKE '%r\%%' ESCAPE '\'
-- 'r%'를 포함하는 데이터
```

### 특수 연산자 - IN

- 지정된 값 중 하나와 일치하는 데이터를 찾음
- OR 조건으로 대체 가능
- 예시
    
    ```sql
    SELECT * FROM EMPLOYEES
    WHERE DEPARTMENT_ID IN (10, 20, 30);
    ```
    

### 특수 연산자 - BETWEEN

- 지정된 범위 내에 있는 값을 찾음
- 범위는 시작 값과 끝 값 포함

```sql
SELECT  * FROM EMPLOYEES
WHERE SALARY BETWEEN 30000 AND 50000;
```

### 특수 연산자 - IS NULL

- NULL 값을 가진 데이터를 찾음
- NOT을 붙이면 NULL 값이 아닌 데이터를 찾음
- 예시

```sql
SELECT * FROM CUSTOMERS WHERE EMAIL IS NULL;
SELECT * FROM CUSTOMERS WHERE EMAIL IS NULL;
```


### 연산자의 우선순위

| 우선순위 | 연산자 |
| --- | --- |
| 1 | () (괄호) |
| 2 | 비교연산자 |
| 3 | 논리연산자(NOT) |
| 4 | 논리연산자(AND) |
| 5 | 논리연산자(OR) |

# 트랜잭션

> 데이터베이스에서 하나의 논리적 작업 단위를 의미
> 

## 1. 트랜잭션의 특징(ACID)

### 원자성(Atomicity)

- 트랜잭션이 DB에 완전히 반영되던지, 아니면 전혀 반영되지 않아야 한다.

### 일관성(Consistency)

- 트랜잭션의 작업 처리 결과가 항상 일관성 있어야 한다.
- 시스템이 가지고 있는 고정 요소는 트랙잭션 적용 전과 후가 같아야 한다.

### 독립성(Isolation)

- 트랜잭션 실행 중에 어떠한 다른 트랙잭션은 연산에 끼어들 수 없다.
- 수행 중인 트랜잭션이 끝날 때까지 다른 트랜잭션에서 수행결과를 참조할 수 없다.

### 지속성(Durability)

- 트랜잭션이 성공적으로 완료되면, 그 결과는 **영구적**으로 데이터베이스에 저장되어야 한다.
- 시스템 오류나 전원 장애가 발생하더라도 트랜잭션 완료 후의 데이터는 보존되어야 한다.

## 2. 트랜잭션 설계 시 유의 사항

- 작업 단위를 명확히 정의: 트랜잭션 범위를 좁게 설계하여 성능과 오류 가능성 최소화
- 최소한의 데이터 변경: 필요 이상의 데이터를 수정하거나 삭제하지 않도록 제한
- 장기 트랜잭션 변경: 트랜잭션이 너무 오래 실행되지 않도록 설계하여 데이터베이스 Lock 문제 방지
- 적절한 격리 수준 설정
