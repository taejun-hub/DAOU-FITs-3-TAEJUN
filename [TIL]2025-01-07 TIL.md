# [2025.01.07]알고리즘과 자료구조-TIL

# 알고리즘

> 어떤 문제를 해결하기 위해 구성된 일련의 절차
> 

**조건**

- 0개 이상의 입력과 1개 이상의 출력이 있어야 함
- 반드시 종료되어야 함
- 모든 명령이 실행 가능해야 함

## 1. 시간복잡도

> 입력 크기(N)에 대해 실행되는 시간을 수학적으로 표현
> 

### 표기법 종류

- 오메가 표기법 - 최선의 경우(Best Case)에서의 시간 복잡도
- 세타 표기법 - 평균적인(Average Case)에서의 시간 복잡도
- 빅오 표기법 - 최악의 경우(Worst Case)에서의 시간 복잡도

## 2. 탐색

### 선형탐색

- 주어진 데이터 집합에서 원하는 데이터를 처음부터 순차적으로 비교하며 찾는 방법
- 시간복잡도: $O(n)$

### 이진탐색

- 정렬된 데이터 집합을 반으로 쪼개 가면서 탐색하는 방법
- 시간복잡도: $O(logn)$

## 3. 정렬

### 선택정렬(Selection Sort)

- 정렬되지 않은 데이터 중에서 가장 작은 데이터를 찾아 가장 앞의 데이터와 교환 해나가는 방식

### 삽입정렬(Insertion Sort)

- 아직 정렬되지 않은 임의의 데이터를 이미 정렬된 부분의 적절한 위치에 삽입하며 정렬하는 방식

### 버블정렬(Bubble Sort)

- 서로 이웃한 데이터들을 비교해 가장 큰 데이터를 맨 뒤로 보내며 정렬하는 방식

### 퀵정렬(Quick Sort)

- 피벗을 중심으로 작거나 같은 값을 지닌 데이터는 앞으로 큰 값을 지닌 데이터는 뒤로 가도록 하여 작은 값을 갖는 데이터와 큰 값을 갖는 데이터로 분리해가며 정렬하는 방법

### 병합정렬(Merge Sort)

- 배열을 반으로 나눈고 나눈 배열들을 재귀적으로 정렬한 뒤, 정렬된 배열들을 병합하여 최종적으로 정렬된 배열을 만듬

### 힙정렬(Heap Sort)

- 힙 자료구조를 사용하여 정렬을 수행

### 정렬 알고리즘 비교

| 정렬 알고리즘 | 최선 시간복잡도 | 평균 시간복잡도 | 최악 시간복잡도 | 공간복잡도 |
| --- | --- | --- | --- | --- |
| 버블 정렬 | O(n) | O(n²) | O(n²) | O(1) |
| 선택 정렬 | O(n²) | O(n²) | O(n²) | O(1) |
| 삽입 정렬 | O(n) | O(n²) | O(n²) | O(1) |
| 퀵 정렬 | O(nlogn) | O(nlogn) | O(n²) | O(logn) |
| 병합 정렬 | O(nlogn) | O(nlogn) | O(nlogn) | O(n) |
| 힙 정렬 | O(nlogn) | O(nlogn) | O(nlogn) | O(1) |

**특징:**

- 버블/선택/삽입 정렬은 단순하지만 성능이 좋지 않아 데이터가 적을 때 사용
- 퀵 정렬은 평균적으로 가장 빠르지만 최악의 경우 O(n²)
- 병합 정렬은 안정적이지만 추가 메모리 공간이 필요
- 힙 정렬은 추가 메모리없이 O(nlogn) 보장

# 자료구조

> 프로그램에서 쉽게 이용할 수 있도록 구성된 데이터 간의 논리적인 관계
> 

## 1. 배열 & 연결리스트

### 배열

**특징:**

- 연속된 메모리 공간에 데이터를 순차적으로 저장
- 인덱스를 통한 빠른 접근 가능 (O(1))
- 고정된 크기를 가진 연속된 메모리 공간에 데이터를 저장

**장점:**

- 데이터 접근이 매우 빠름
- 캐시 지역성이 좋아 메모리 효율적
- 구현이 간단함

**단점:**

- 크기를 변경하기 어려움
- 중간에 데이터 삽입/삭제가 비효율적 (O(n))
- 메모리 공간 낭비가 발생할 수 있음

### 연결 리스트

**특징:**

- 노드들이 포인터로 연결되어 데이터를 저장
- 데이터 영역에 여러 개의 데이터가 저장될 수 있음
- 각 노드들은 주기억장치의 어느 위치에 저장되든 상관없다
- 요소들이 메모리 상에서 연속적이지 않고, 각각의 노드는 동적으로 할당되어 연결

**장점:**

- 데이터의 삽입/삭제가 효율적 (O(1))
- 메모리를 동적으로 할당하여 효율적 사용 가능
- 크기의 제한이 없음

**단점:**

- 특정 위치의 데이터 접근에 시간이 많이 소요 (O(n))
- 포인터를 위한 추가 메모리 공간이 필요
- 캐시 지역성이 낮음

**단순연결리스트** - 노드에 포인터 영역이 하나인 연결 리스트

**이중연결리스트** - 각 노드에 다음 노드를 가리키는 포인터 영역과 이전 노드를 가리키는 포인터 영역을 포함

## 2. 스택 & 큐

### 스택(Stack)

- 데이터의 삽입과 삭제가 한쪽 방향에서만 일어나는 구조
- 가장 나중에 삽입된 데이터가 가장 먼저 삭제
- 후입선출(Last - In - First - Out)

### 큐(Queue)

- 데이터가 한쪽 방향으로 삽입되고 반대 방향으로 삭제되는 구조
- 가장 먼저 삽입된 데이터가 가장 먼저 삭제
- 선입선출(First - In - First - Out)

### 우선순위 큐(Priority Queue)

- 각 요소가 우선순위를 가지며, 큐에서 꺼낼 때 우선순위가 높은 요소부터 꺼내는 자료구조
- 구현방법
    1. 배열 또는 리스트: 삽입할 때 마다 배열을 정렬하여 우선순위가 높은 값이 먼저 나가도록 함, 하지만 삭제시 배열을 매번 정렬해야 하기 때문에 비효율적
    2. 힙(Heap): 최대힙, 최소힙을 사용하면 삽입과 삭제 연산 모두 효율적으로 처리 가능

## 3. 그래프

> 정점과 간선으로 구성된 자료구조, 서로 연결된 관계를 표현하는 데 사용
> 

### 그래프의 종류

- 무방향 그래프 - 방향성이 없는 간선으로 이루어진 그래프
- 방향 그래프 - 방향성이 있는 간선으로 이루어지 그래프
- 완전 그래프 - 모든 정점 사이에 간선이 있는 그래프
- 가중 그래프 - 간선마다 가중치를 부여한 그래프
- 다중 그래프 - 두 정점을 연결하는 다중간선이 존재
- 유사 그래프 - 다중간선과 루프를 포함

### 그래프의 Multiplicity(다중도)

그래프에서 두 정점을 연결하는 간선의 개수

- 단순 그래프: 두 정점 사이의 간선이 최대 1개인 그래프 (multiplicity ≤ 1)
- 다중 그래프: 두 정점 사이에 여러 개의 간선이 존재할 수 있는 그래프 (multiplicity > 1)

### 인접행렬 (Incidence Matrix)

> 인접행렬은 정점과 간선 사이의 관계를 나타내는 행렬
> 
- 정점의 개수가 n이고 간선의 개수가 m인 그래프를 n × m 크기의 행렬로 표현
- 행은 정점을, 열은 간선을 나타냄

**무방향 그래프의 경우:**

- 간선에 연결된 정점은 1, 연결되지 않은 정점은 0으로 표시
- 각 열(간선)에는 정확히 2개의 1이 존재 (간선의 양 끝점)

**방향 그래프의 경우:**

- 시작 정점은 -1, 도착 정점은 1, 연결되지 않은 정점은 0으로 표시
- 각 열에는 하나의 -1과 하나의 1이 존재

**특징:**

- 희소 그래프의 경우 많은 0을 포함하여 메모리 낭비가 발생할 수 있음
- 간선의 존재 여부를 확인하는데 O(m) 시간이 필요
- 그래프의 구조 변경(간선 추가/삭제)이 용이

## 4. 이진 트리

> 모든 노드들의 자식 노드가 두 개 이하인 트리, 배열이나 연결리스트로 구현 가능
> 
- **완전 이진 트리**  이진 트리 중에서 단말 노드를 제외한 나머지 노드가 모두 두 개의 자식 노드를 가지고 있는 트리
- **포화 이진 트리** - 완전 이진 트리 중에서 모든 노드가 채워진 이진 트리

### 이진 트리 순회

- 전위순회(Pre-order): 루트 → 왼쪽 → 오른쪽
- 중위순회(In-order): 왼쪽 → 루트 → 오른쪽
- 후위순회(Post-order): 왼쪽 → 오른쪽 →루트

### 이진 탐색 트리(Binary Search Tree)

- 이진 트리 중에서 같은 데이터를 갖는 노드가 없다.
- 왼쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 작다.
- 오른쪽 서브 트리에 있는 모든 데이터가 현재 노드의 데이터보다 크다.
- 이진 탐색 트리가 균형을 이루기 위해서는 트리의 높이를 가능한 한 작게 유지해야함(트리의 높이가 log n에 가까워야 한다.
- 다양한 자기 균형 이진 탐색 트리 알고리즘들을 사용할 수 있음(AVL 트리, 레드-블랙 트리)

## 5. 힙(Heap)

> 완전 이진 트리를 기본으로 한 자료구조로, 부모 노드와 자식 노드 간의 대소 관계가 정해져 있는 자료구조
> 

### 힙의 종류

- **최대 힙(Max Heap)** - 부모 노드의 값이 자식 노드의 값보다 크거나 같은 완전 이진 트리
- **최소 힙(Min Heap)** - 부모 노드의 값이 자식 노드의 값보다 작거나 같은 완전 이진 트리

### 힙의 특징

- 완전 이진 트리의 형태를 만족해야 함
- 중복된 값을 허용함
- 배열을 사용하여 구현할 수 있음
- 우선순위 큐를 구현하는데 주로 사용됨

### 힙의 연산

- **삽입(Insert)** - 새로운 노드를 마지막 위치에 추가한 후, 부모 노드와 비교하며 교환하는 과정을 반복 (시간복잡도: O(log n))
- **삭제(Delete)** - 루트 노드를 삭제하고, 마지막 노드를 루트로 이동시킨 후, 자식 노드와 비교하며 교환하는 과정을 반복 (시간복잡도: O(log n))

### 힙의 활용

- 우선순위 큐 구현
- 힙 정렬
- 다익스트라 알고리즘과 같은 최단 경로 알고리즘
- 시스템의 작업 스케줄링

### 몬테카를로 트리(Monte Carlo Tree)

> 확률적 샘플링을 통해 의사결정을 하는 트리 구조의 알고리즘
> 

**주요 활용 분야**

- 게임 AI ****- 바둑, 체스와 같은 복잡한 게임에서 다음 수를 결정하는데 사용
- 경로 계획 - 로봇 내비게이션이나 자율주행 시스템의 경로 결정
- 강화학습 - 알파고와 같은 AI 시스템의 의사결정 과정
- 최적화 문제 - 복잡한 스케줄링이나 리소스 할당 문제 해결

**특징**

- 불확실성이 높은 상황에서 효과적인 의사결정 가능
- 실시간 계산이 필요한 상황에서 유용
- 탐색 공간이 매우 큰 문제에 적합

## 6. 해시 테이블

### 해시 테이블의 특징

- 해시 테이블은 일반적으로 배열을 기반으로 하며, 키를 해시 함수를 통해 인덱스로 변환하여 데이터를 저장하고 검색
- 평균적으로 O(1)의 시간복잡도로 데이터를 검색, 삽입 및 삭제 가능

### 해시 테이블의 충돌

- 해시 함수가 서로 다른 키들에 대해 동일한 해시값을 반환하는 경우 충돌이 발생

### 해시 충돌 해결 방법

- 체이닝(Chaining): 같은 인덱스에 여러 값을 저장할 수 있도록 연결 리스트를 사용하여 충돌을 해결
- 개방 주소법(Open Addressing): 충돌이 발생하면 다른 빈 슬롯을 찾아 데이터를 삽입하는 방식
