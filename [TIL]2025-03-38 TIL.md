## 1. 콘솔에 눈에 띄게 출력하는 방법 3가지

| 방법 | 설명 | 콘솔 호환 |
| --- | --- | --- |
| ASCII + 이모지 + 구분선 | `printf("\n====...")` | 모든 플랫폼 |
| Windows 색상 API | `SetConsoleTextAttribute()` 사용 | Windows 전용 |
| ANSI 색상 코드 | `\033[1;31m` 등 사용 | Linux / PowerShell 일부 지원 |

각각 `print_stock_amount_alert()`, `print_account_amount_alert()`, `print_wash_sale_alert()` 함수로 분리했음.

---

## 2. Oracle DB에 주문 삽입 후 결과 출력

- `add_order(StockOrder *order)` 함수에서 `order_id`를 `RETURNING INTO :7`로 받아오는 구조 완성
- 삽입 후 `order->status = PENDING`, `created_at`은 `time(NULL)` 기준으로 채움
- `print_order(order)` 함수를 새로 만들어 1개 주문을 예쁘게 출력

---

## 3. 콘솔 핸들 관련

- `GetStdHandle(STD_OUTPUT_HANDLE)`로 얻은 `hConsole`은 **해제할 필요 없음**
- Windows 시스템이 프로세스 종료 시 자동으로 정리해 줌
- 직접 `CloseHandle()` 하면 콘솔 출력을 잃을 수 있음 (주의)

---

## 4. 부가 구현

- `print_orders()`는 다수 출력용
- `print_order()`는 1개용으로 리팩터링함
- `get_account_by_id()`로 계좌 상태 확인하여 주문 가능 여부 체크

---
