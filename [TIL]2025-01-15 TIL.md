# 금융 시스템의 이해

## 1. 금융 시스템

- 자금 흐름을 관리하고 지원하는 시스템

### 주요 구성요소

- 은행 시스템
    - 계좌 권리, 대출, 결제 서비스
- 증권 시스템
    - 주식, 채권, 펀드 거래 및 관리
- 보험 시스템
    - 보험 가입, 계약 관리, 보상 처리
- 결제 시스템
    - 카드 결제, 온라인 송금, 전자 화폐

### 은행 시스템

- 고객 관리 시스템(CRM)
    - 고객 정보, 계좌 고란리
- 대출 관리 시스템
    - 대출 신청, 심사, 상환 관리
- 결제 처리 시스템
    - 송금, 카드 결제, 자동이체
- 리스트 관리 시스템
    - 신용 평가, 부도 예측, 규제 준수

### 증권 시스템

- 거래 시스템
- 결제 및 청산 시스템
- 포트폴리오 관리 시스템
- 리스크 관리 시스템

### 보험 시스템

- 보험 계약 관리
- 청구 및  보상 시스템
- 고객 서비스 시스템
- 리스크 관리 시스템

# SQL

## 1. ORDER BY

- SQL에서 데이터를 정렬하는데 사용되는 절
- 기본적으로 오름차순(ASC) 정렬
- 내림차순 정렬 시 DESC 키워드 사용
- 하나 이상의 컬럼을 기준으로 정렬 가능
- **기본 문법**

```sql
SELECT 컬럼명 FROM 테이블명
ORDER BY 컬럼명 [ASC][DESC];
```

### 다중 컬럼 정렬

- 여러 컬럼을 기준으로 정렬 가능
- 우선순위는 지정한 컬럼의 순서에 따름
- 예시
    
    ```sql
    SELECT NAME, DEPARTMENT_ID, SALARY FROM EMPLOYEES
    ORDER BY DEPARTMENT_ID ASC, SALARY DESC;
    ```
    

### ORDER BY 와 NULL

- Oracle에서 NULL 값은 최대값으로 간주
- `NULLS FIRST` 또는 `NULLS LAST`로 명시적 설정 가능
- 예시
    
    ```sql
    SELECT NAME, SALARY FROM EMPLOYEES
    ORDER BY SALARY ASC NULLS FIRST;
    ```
    

## 2. 페이징(Paging)

> 대량의 데이터에서 일정한 크기로 데이터를 나누어 보여주는 방법
> 

### 페이징 쿼리 개념

- `ROWNUM`: 오라클에서 결과 집합의 각 행에 부여되는 번호, 슈도 컬럼
- `ROW_NUMBER()`: 오라클의 윈도우 함수로, 정렬 기준에 따라 고유 번호를 부여

### 페이징 쿼리 작성 방법

- 예시

```sql
SELECT * FROM (
	SELECT ROW_NUMBER() OVER
		(ORDER BY column_name) AS row_num, column_list 
	FROM table_name
)
WHERE row_num BETWEEN :start_row AND :end_row;
```

- 정렬 기준
    - ORDER BY는 페이징 쿼리에서 필수
    - 정렬 기준이 없으면 결과 순서가 비정상적으로 출력될 수 있음
- 성능 고려
    - 대규모 데이터셋에서 OFFSET 사용 시 성능 저하 가능성 존재(상황이 따라 다르다)
    - 인덱스를 적절히 설정하여 쿼리 성능 최적화
- 다양한 페이지 크기 테스트
    - 다양한 크기의 페이지를 고려하여 쿼리 설계

## 3. DISTINCT

- SQL에서 중복된 데이터를 제거하여 고유한 값만 반환하는 키워드
- SELECT 문과 함꼐 사용하여 특정 컬럼의 고유한 값만 조회할 수 있음
- DISTINCT 다음에 여러 개의 컬럼이 나올 경우 모두 중복의 기준에 적용됨
- 기본 문법

```sql
SELECT DISTINCT 컬럼명 FROM 테이블명
WHERE 조건;
```

## 4. GROUP BY

- SELECT 문에서 데이터를 그룹으로 묶는 절
- 그룹별로 집계 결과를 반환
- WHERE 절과 함께 사용 가능
- 기본 문법

```sql
SELECT 컬럼명, 집계함수 FROM 테이블명
GROUP BY 컬럼명;
```

### 집계함수

- COUNT(): 행의 개수 반환
- SUM(): 합계 반환
- AVG(): 평균 반환
- MAX(): 최대값 반환
- MIN(): 최소값 반환

### 예제

```sql
-- 직책별 최대 급여와 최소 급여 계산
SELECT JOB_ID, 
    MIN(SALARY) AS MIN, 
    MAX(SALARY) AS MAX 
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY JOB_ID
ORDER BY JOB_ID;
```

### GROUP BY와 WHERE

- WHERE 절은 GROUP BY 전에 조건을 필터링

### GROUP BY와 HAVING

- HAVING 절은 GROUP BY 이후 그룹별 조건 필터링
- 예시

```sql
SELECT DEPARTMENT_ID, AVG(SALARY) AS AVG
FROM EMPLOYEES
WHERE DEPARTMENT_ID IS NOT NULL
GROUP BY DEPARTMENT_ID
HAVING AVG(SALARY) >= 9000
ORDER BY DEPARTMENT_ID;
```

### 다중 컬럼 GROUP BY

- 각 컬럼이 모두 같아야 한 그룹이 된다.
- 예시
    - 부서 및 직무별 급여 합계
        - 각 부서 (DEPARTMENT_ID)와 직무(JOB_ID)별로 급여 합계를 계산
        
        ```sql
        SELECT DEPARTMENT_ID, JOB_ID, SUM(SALARY) 
        FROM EMPLOYEES
        GROUP BY DEPARTMENT_ID, JOB_ID
        ```
        

## 5. 함수

### 단일 행 함수

> 한 행에 대해 하나의 결과를 반환
> 
- 문자 함수: 문자열 변환 및 처리
    - 문자열을 다루기 위한 함수
    - 주요 함수
        - UPPER(), LOWER(): 대소문자 변환
        - LENGTH(), SUBSTR(): 문자열 길이와 문자열 일부 추출
            - 예시
            
            ```sql
            SELECT LENGTH('SQL Fuction') AS LENGTH,
                SUBSTR('SQL Fuction', 5, 8) AS SUBSTRING
                --- SUBSTR(문자열, 시작위치, 자를 문자열 길이)
            FROM DUAL;
            ```
            
        - INSTR(), TRIM(): 특정 문자열의 처음 위치 출력과 공백 제거
        - LPAD(), RPAD(): 문자열 채움
        - REPLACE()와 CONCAT(): 문자열 대체와 두 문자열 연결
            - COCAT() 대신 `||` 사용 가능
- 숫자 함수: 수학적 계산
    - 주요 함수
        - ROUND(), TRUNC(): 숫자의 반올림과 자르기
        - CEIL(), FLOOR(): 숫자의 올림과 내림
        - ABS(), MOD(): 절대값과 나머지 계산
- 날짜 함수: 날짜 및 시간 처리
    - 주요 함수
        - SYSDATE, CURRENT_DATE: 현재 날짜와 시간 반환
        - ADD_MONTHS(), MONTHS_BETWEEN(): 날짜 계산
        - NEXT_DAY(), LAST_DAY(): 지정된 요일의 다음 날짜, 월의 마지막 날짜
        - EXTRACT(), TRUNC(): 날짜에서 특정 요소 추출/ 날짜를 특정 단위로 자름
- 변환 함수: 데이터 형식 변환
    - 주요 함수
        - TO_CHAR()
        - TO_DATE()
        - TO_NUMBER()
- NULL 처리 함수: NULL 값 처리
    - 주요 함수
        - NVL(): NULL 값 대체
        - NVL2(): 여부에 따라 다른 값 반환
            - 예시: `NVL2(값, NULL이 아닐때 반환값, NULL일때 반환값)`
        - COALESCE(): NULL이 아닌 첫 번째 값 반환
        - NULLIF(): 두 값이 같으면 NULL 반환

### 집계 함수

> 여러 행을 그룹으로 묶어 하나의 결과를 번환
> 

## 6. CASE WHEN과 DECODE

### CASE WHEN

- SQL에서 조건에 따라 값을 반환하는 문법
- SELECT, UPDATE, DELETE 문 등에서 사용 가능
- 가독성이 뛰어나 복잡한 조건을 처리할 때 유용
- **장점**
    - 조건 처리에 유연함
    - 여러 조건 및 복잡한 로직 처리 가능
- 기본 문법

```sql
-- 방식 1
CASE
    WHEN 조건1 THEN 결과1
		WHEN 조건2 THEN 결과2
    ELSE 결과3
END 

-- 방식 2
CASE 컬럼명
    WHEN 값 THEN 결과1
		WHEN 값 THEN 결과2
    ELSE 결과3
END 
```

### DECODE

- 특정 값에 따라 다른 값을 반환하는 함수
- **장점**
    - 간단한 값 매칭에 적합
    - 특정 열의 값 기반으로 처리
- 기본 문법
    
    `DECODE(컬럼명, 조건1, 결과1, 조건2, 결과2, ...., 기본값)`
    

## 7. JOIN

> 여러 테이블의 데이터를 결합하여 하나의 결과 집합을 생성하는 SQL절
> 

### 종류

- INNER JOIN
- OUTER JOIN
- SELF JOIN
- CROSS JOIN
- NATURAL JOIN

### INNER JOIN

- 두 테이블 간에 일치하는 데이터만 반환
- 기본적이 JOIN 방식
- 예시

```sql
-- 방식 1
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E
    INNER JOIN DEPARTMENTS D ON E.DEPARTMENT_ID = D.DEPARTMENT_ID
 
-- 방식 2
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E, DEPARTMENTS D
WHERE E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### LEFT OUTER JOIN

- LEFT 테이블의 모든 행과 일치하는 RIGHT 테이블의 데이터를 반환
- 일치하지 않은 경우 NULL 반환
- 예시

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E 
LEFT OUTER JOIN DEPARTMENTS D
	ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### FULL OUTER JOIN

- LEFT 테이블과 RIGHT 테이블의 모든 행 반환
- 일치하지 않은 데이터는 NULL로 표시
- 예시

```sql
SELECT E.NAME, D.DEPARTMENT_NAME
FROM EMPLOYEES E 
    FULL OUTER JOIN DEPARTMENTS D
        ON E.DEPARTMENT_ID = D.DEPARTMENT_ID;
```

### SELF JOIN

- 같은 테이블을 두 번 사용하여 JOIN 수행
- 주로 계층 구조를 표현할 때 사용
- 예시

```sql
SELECT E1.NAME EMPLOYEE, E2.NAME AS MANAGER
FROM EMPLOYEES E1 
    JOIN EMPLOYEES E2
        ON E1.EMPLOYEE_ID = E2.EMPLOYEE_ID
```

### CROSS JOIN

- 두 테이블 간의 모든 행의 조합을 반환(Cartesian Product)
- JOIN 조건이 없는 경우 발생
- 결과 집합의 크기가 매우 커질 수 있으므로 조심해서 사용
- 예시

```sql
SELECT E.NAME,D.DEPARTMENT_NAME
FROM EMPLOYEES E
    CROSS JOIN DEPARTMENTS D;
```

### NATURAL JOIN

- 동일한 이름의 컬럼을 기준으로 JOIN
- 조건을 명시하지 않아도 자동으로 연결
- 예시

```sql
SELECT NAME,DEPARTMENT_NAME
FROM EMPLOYEES
    NATURAL JOIN DEPARTMENTS;
```

### JOIN 사용 시 유의사항

- JOIN 조건 누락
- NULL 처리
    - OUTER JOIN시 NULL 값을 처리하는 로직 필요
- 성능
    - 큰 테이블 간 JOIN은 성능 저하 기능, 적절한 인텍스 설정 필요
