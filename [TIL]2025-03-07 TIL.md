# 제어문

## _getch() 함수

> **사용자가 키보드에서 키를 누를 때까지 기다렸다가, 해당 키 값을 즉시 반환하는 함수**야.
> 

입력된 문자를 화면에 출력하지 않고 **버퍼 없이 즉시 처리할 때 유용**.

(Visual Studio의 **`conio.h` 헤더 파일**에 포함되어 있음)

---

### **1. _getch() 함수 사용법**

```c

#include <stdio.h>
#include <conio.h> // _getch() 사용을 위한 헤더

int main() {
    char ch;
    printf("아무 키나 누르세요: ");

    ch = _getch();  // 키 입력 대기 (화면에 출력되지 않음)

    printf("\n입력한 키: %c\n", ch);
    return 0;
}

```

✅ **입력한 키가 화면에 표시되지 않음** → 비밀번호 입력 같은 기능에 활용 가능!

---

### **2. _getch() vs _getche() vs getchar() 비교**

| 함수 | 설명 | 입력된 문자 출력 여부 | 엔터 필요 여부 |
| --- | --- | --- | --- |
| `_getch()` | 입력한 키를 즉시 반환 | ❌ (출력 안 됨) | ❌ |
| `_getche()` | 입력한 키를 즉시 반환 | ✅ (출력됨) | ❌ |
| `getchar()` | 입력한 키를 반환 (버퍼 사용) | ✅ (출력됨) | ✅ (엔터 필요) |

# 함수

## 1. 함수의 종류

### 표준함수

- 내장함수 또는 시스템 제공 함수라고 하며, 미리 정의되어 있어 프로그래머가 호출하여 사용할 수 있는 함수

### 사용자 정의 함수

- 개발자가 직접 작성하는 함수로, 코드 재사용성과 가독성을 높이는 데 사용된다

### 가변인자 함수

- 매개변수 개수가 고정되지 않은 함수로, `stdarg.h` 라이브러리를 사용해야 함.

```c

#include <stdio.h>
#include <stdarg.h>

// 여러 개의 숫자를 더하는 함수
int sum(int count, ...) {
    va_list args;
    va_start(args, count);

    int total = 0;
    for (int i = 0; i < count; i++) {
        total += va_arg(args, int);
    }

    va_end(args);
    return total;
}

int main() {
    printf("합계: %d\n", sum(3, 10, 20, 30)); // 출력: 60
    return 0;
}
```

- 대표적인 가변인자 함수
    - `printf()`
    - `scanf()`

### 재귀함수

- 자기 자신을 호출하는 함수

```c
int fibo(int n) {
	if (n == 1) {
		return 0;
	}
	else if (n == 2) {
		return 1;
	}
	return fibo(n - 1) + fibo(n - 2);
}
```
