# Java언어의 특징

- 운영체제에 독립적
- 객체지향 언어
- 하이레벨 랭귀지로 작성한 코드를 컴파일러를 통해서 머신코드로 변경
- 자바는 컴파일러를 통해서 바이트코드로 변경
- 변경된 바이트 코드를 JVM이 읽어드려서 머신코드로 변경해서 실행
- .java → (javac compiler) → .class → (JVM)
- 프로그램을 실행하기 위한 다양한 API 제공

## Java의 객체지향

### **절차적 프로그래밍**

- 대표적인 언어가 C
- 기능으로 프로그램을 세분화
- 더이상 분할 할 수 없는 단위기능이 나오게 됨
- 이 단위기능을 함수로 구현

**장점**

- 설계와 구현이 쉽고 결과적으로 개발에 있어서 비용적인 이점이 있음

**단점**

- 유지보수가 힘들다

절차적 프로그래밍의 단점을 해결하기 위한 프로그래밍 패러다임이 변화 ⇒ 객체지향 프로그래밍

### 객체지향 프로그래밍

> 현실세계의 구현해야 하는 문제를 프로그래밍적으로 표현(모델링)을 하는 구현 방법론
> 
- 해결해야 하는 문제를 구성하고 있는 `구성요소`를 파악
- 추상화(Absrtraction)을 거쳐서 나온 여러가지 정보를 `상태`(잔액, 나이, 키, 주소, 주민번호, …)와 `행위` (이체, 출금, 대출, …)로 구분
- 상태는 변수(fields)로 행위는 함수(method)로 표현
- 이 field와 method는 class라는 단위로 묶여서 표현
- class라는 단위는 객체 모델링을 하기 위한 수단으로 사용
- class의 첫번째 의미는 `객체 모델링`의 수단
- 자바프로그램은 `class의 집합`으로 표현될 수 있다
- 코드로 표현할 때 class 1개당 파일 1개를 매핑
- 클래스의 이름을 파일이름으로 명명(public class일 경우)
- 클래스 이름은 첫글자 대문자

```java
class Customer {
		
		//fields
		String name; // 고객이름
		int age;     // 고객나이
		
		//method
}
```

- 이 클래스로부터 저장공간을 생성해서 그 저장공간을 이용해서 프로그래밍을 진행
- 이 저장공간을 `instance`라고 한다
- 클래스는 데이터 타입의 역할을 수행
- 클래스의 두번째 의미는 `ADT(Abstract Data Type)`
- 클래스의 세번째 의미는 `instance`를 파생시키는 수단
- 하나의 프로그램 내에는 상당히 많은 class들이 존재
- 클래스를 효율적으로 관리하기 위해 논리적인 개념 도입 ⇒ package
- package는 물리적으로 폴더로 표현된다

# JVM, JRE, JDK

## JVM(Java Virtual Machine)

- 자바 프로그램을 실행하기 위한 가상 머신
- bytecode를 읽어들여서 실행하는 실행 주체

## JRE(Java Runtiome Environment)

- 자바실행환경
- JVM + Java Class Library

## JDK(Java Delvelopment Kit)

- 자바개발도구
- JRE + 자바개발도구(javac.exe, java.exe, javadoc.exe, etc…)

# (Java) Data Type

## 1. variable

- Java는 `Strong type language`로 변수를 선언할 때 변수의 데이터 타입을 지정해서 변수를 선언
- Java는 변수 길이의 제한이 없음
- 예약어로는 변수를 만들 수 없음
- 숫자로 시작하는 변수는 만들 수 없음
- 특수문자는 `_`와 `$`만 허용
- 클래스 이름은 pascal case ⇒ MyFriend
- 일반 변수 이름은 camel case ⇒ myFriend
- 상수의 이름은 모두 대문자, 여러 단어로 이루어진 경우 `_`로 구분(snake case)

## 2. 데이터 타입

### primitive data type

- 8개가 정해져 있다
- 해당 타입으로 선언된 변수 안에 직접 데이터가 저장(스택에도 저장될 수 있고 힙에도 저장될 수 있다)

|  | 1 byte | 2 byte | 4 byte | 8 byte |
| --- | --- | --- | --- | --- |
|  논리형 | boolean |  |  |  |
| 문자형 |  | char |  |  |
| 정수형 | byte | short | int | long |
| 실수형 |  |  | float | double |
- 문자형은 내부적으로 정수(`유니코드`)로 저장하기 때문에 정수형과 별반 다르지 않고, 정수형 또는 실수형과 연산도 가능

### reference data type

- `클래스`로 선언된 변수를 지칭
- 선언된 변수 안에는  메모리 주소(실제로는 해쉬값, 직접적인 접근을 막기위해)만 저장

## 3. 상수(constant)

> `final int MAX_SPEED = 100;`
> 
- final이라는 키워드로 상수를 표현

## 4. 형변환(Type Casting)

> 하나의 데이터 타입이 다른 데이터 타입으로 변환하는걸 의미
> 

### 묵시적 형변환(implicit)

> `double k = 10 + 20.0;`
> 
- 연산을 위해 10이 묵시적으로 double 타입으로 바뀐다(안전할 경우만)

### 명시적 형변환(casting)

> `(인용)피연산자`
> 

```java
public class InstanceTest {

    // static field or static variables or class variable (static은 인스턴스가 없어도 사용 가능)
    static int a = staticCall("1번"); // method area에 생성

    // instance varible
    int b = staticCall("2번");

    //static method
    static public int staticCall(String msg) {
        System.out.println(msg);
        return 100;
    }

    // constructor
    // 기본 생성자는 무조건 작성을 하는게 좋다
    // 생성자는 인스턴스를 생성하기 위해서 반드시 호출되고
    // 만들어진 인스턴스를 초기화하는 역할
    // this는 this를 사용하고 있는 객체의 reference
    public InstanceTest() {
        this.b = staticCall("3번");
    }

    // entry point
    public static void main(String[] args) {
        System.out.println("4번");
        int c = staticCall("5번");
        InstanceTest d = new InstanceTest();
    }
}

// 실행 결과: 1 -> 4 -> 5 -> 2 -> 3
```

# (Java) 객체지향

## 1. Method Overloading

### Overloading

> 같은 이름이나 같은 형태로 여러개의 기능을  수행하는 경우
> 

- 같은 이름의 method가 여러 개 존재하는 것을 의미
- method가 갖는 매개변수의 개수와 데이터 타입을 이용하여 이름은 같지만 서로 다른 method로 구분하는 개념
- 생성자에도 적용 가능(Constructor Overloading)

```java
public class OverloadingTest {

    // Constructor Overloading
    public OverloadingTest() {
        System.out.println("Overloading Test1");
    }
    public OverloadingTest(int a) {
        System.out.println("Overloading Test2");
    }

    public static void main(String[] args) {
        OverloadingTest o1 = new OverloadingTest();

        OverloadingTest o2 = new OverloadingTest(1);
    }
}
```
