# 컴퓨터 네트워크

> 컴퓨터 여러 대가 데이터를 주고받는 통신 컴퓨터시스템
> 

### 컴퓨터 네트워크 종류

- 근거리 네트워크(LAN)
- 장거리 네트워크(WAN)
- 중거리 네트워크(MAN)
- 개인 네트워크(PAN)
- 초근거리 네트워크(NFC)

### LAN 토폴로지

- 컴퓨터를 연결하는 LAN의 구조, 위상 배치에 따라 3가지로 나눔
1. **링형**
    - 장치들을 원형으로 연결
    - 데이터 전송을 위해 토큰(token) 사용
2. **버스형**
    - 하나의 통신 회선에 장치를 연결
    - 데이터를 목적지 주소와 함께 버스에 실어보냄
    - 문제점: 메인 통신 회선에 문제가 생기면 전체에 문제 발생
3. **스타형**
    - 중앙에 위치한 한 장치에 모든 장치를 연결
    - 항상 중앙 장치를 통해 데이터 전송이 이루어짐

### 가상 사설망(VPN, Virtual Private Network)

> 인터넷과 같은 공용 네트워크를 사설 네트워크에 안전하게 연결할 수 있도록 하는 기술
> 
- 사용자가 인터넷을 통해 원격으로 연결된 네트워크에 안전하게 접근할 수 있게 해준다.
- 데이터가 암호화되어 전송되기 때문에 정보 유출이나 해킹으로부터 보호

### 통신의 분류

- 통신 방향에 따라
    - 단방향 통신
    - 양방향 통신
    - 반양방향 통신
- 통신 대상에 따라
    - 유니캐스트 - 일대일 연결
    - 브로드캐스트 - 보내는 기기 한 대와 불특정 다수를 연결
    - 멀티캐스트 - 보내는 기기 한 대와 받는 기기 여러 대를 연결

### OSI 7계층 모델

1. 물리 계층 - LAN에서 통신기기의 연결을 담당
2. 데이터 링크 계층 - 인접한 기기 간에 오류 없이 데이터를 전송하도록 제어
3. 네트워크 계층 - 여러 LAN을 하나로 묶어서 인터넷을 만드는 핵심 계층
4. 전송 계층 - 네트워크 계층에서 목적지까지 오면서 생기는 오류를 보정
5. 세션 계층
6. 표현 계층 
7. 응용 계층- 인터넷을 사용하는 각종 응용 프로그램이 위치하는 계층

- 5계층 모델에 세션 계층, 표현 계층이 추가됨
- 계층의 독립성
    - 각 계층은 자신보다 상위 계층의 작업을 알지 못함
    - 각 계층은 같은 계층끼리만 통신

# TCP/IP

> 인터넷의 기초 프로토콜, 네트워크로 연결된 컴퓨터 간의 메시지를 안정적으로 전달하는 데 사용됨
> 

## 1. 물리 계층

> 실제 전송 매체에서 데이터를 전기적 신호나 광신호로 전송하는 프로토콜
> 

### 통신선

- 꼬임선(Twisted Pair): 두 개의 전선을 꼬아 외부의 영향을 덜 받게함
- 동축선(Coaxial Cable): 중앙에 구리선이 있고 이를 내부 피복으로 감싼 구조
- 광섬유(Optical Fiber): 가느다란 선에 빛을 이용하여 데이터를 전송

## 2. 데이터 링크 계층

> 같은 네트워크 내에서 패킷을 물리적 주소로 전송
> 
1. 프레임(frame): 데이터 링크 헤더 + 패킷
2. 전송방식
    1. Ethernet
    2. Wi-Fi

### MAC 주소

> 데이터 링크 계층에서 사용되는 물리적 수소
> 
- 네트워크 인터페이스 카드(NIC)나 네트워크 장치에 고유하게 할당
- 48비트 길이로 12자리의 16진수로 표현.
- 첫 6자리는 제조사 식별자, 나머지 6자리는 각 장치에 고유한 식별 번호

### 오류를 처리하는 방식

1. 데이터 중복 전송 - 송신측에서 수신측으로 각 데이터를 두 번씩 보내는 방법
2. 중복 정보 전송 - 전송할 데이터에 대한 중복 정보를 데이터와 함께 보내는 방법
3. 오류 탐색 코드(오류를 찾는다) - 패리티 비트, 체크섬, CRC코드
4. 오류 보정 코드(허밍 코드) - 허밍 코드

### 패리티 비트(Parity Bit)

- 보내려는 데이터에 1비트를 추가하여 전체 1의 개수가 짝수 혹은 홀수가 되도록 만드는 방식
- 오류가 짝수 개 발생하면 오루를 찾지 못함

### 체크섬(Check Sum)

- 보내려는 데이터를 일정 크기로 자르고 이를 모두 더하여 코드를 만듬
- 송신측의 TCP(또는 UDP)에서 전송할 데이터에 대한 체크섬을 구해 TCP 헤더에 포함해서 전송
- 수신측의 TCP에서 TCP 헤더에 포함된 체크섬을 검사해 수신한 데이터에 오류가 있는지 확인

### 순환 중복 검사(CRC)

- 모듈로 나눗셈 결과 나머지를 구해 중복 정보로 만듬
- 수식측에서는 전송받은 정보를 동일한 n + 1 제수로 나눗셈을 실시하여 나머지가 0이면 오류가 없는 것으로 간주, 나머지가 0이 아니면 오류로 간주

## 3. 네트워크 계층

> 데이터를 목적지까지 라우팅하고 전달하는 역할
> 
- IP: 네트워크 사이에서 데이터를 주고받는 규칙을 정한 프로토콜
- TCP: 데이터를 전송할 때 생기는 오류를 바로잡고, 데이터를 목적지 프로그램에 전달
- 헤더: 데이터 전송을 위해 추가되는 자료구조
- 라우팅: 어떤 길로 패킷을 보낼지 결정
- 라우터: 패킷을 어느 쪽 네트워크로 보낼지 결정하는 통신 장비
- 패킷: 네트워크 계층에서 다루는 데이터 단위
- 프로토콜: 네트워크 통신에서 데이터를 주고받을 때의 규칙과 절차를 정의한 것

### IP주소

- 인터넷에 연결된 여러 장치를 식별하는 번호
- 네 개의 10진수로 구성, 10진수는 여덟 자리 2진수를 의미
- 0.0.0.0 부터 255. 255.255.255 까지 가능 총 2의 32제곱
- 네트워크 주소와 호스트 주소로 구분 가능
- 클래스
    - 클래스 A
    - 클래스 B
    - 클래스 C

### 서브넷 마스크(Subnet Mask)

- IP 네트워크에서 서브넷을 나누기 위해 사용
- 처음 24비트가 네트워크 주소, 나머지 8비트가 호스트 주소
- 예시
    - /24 서브넷 마스크
        - IP주소: 192.168.1.10
        - 서브넷 마스크: 255.255.255.0
        - 네트워크 주소: 192.168.1.0
        - 호스트 주소: 192.168.1.10

### IP 헤더

- 헤더의 크기: 20바이트
- 필드 구성

### MAC 헤더

- 데이터를 보낼 상대의 IP 주소가 정해지면, 상대 네트워크 카드의 MAC 주소를 포함하고 있는 MAC 헤더를 추가
- 헤더의 크기: 14바이트

### 라우팅 테이블

### 게이트웨이(Gateway)

- 컴퓨터 네트워크에서 다른 네트워크와 연결되도록 도와주는 장치

### 허브

### 스위치

### 동적 라우팅 프로토콜

- RIP
    - 거리 벡터 기반의 라우팅 프로토콜
    - 목적지까지의 거리를 홉 수(hop count)로 측정. 최대 홉 수 15로 제한
- OSPF
    - 링크 상태(link state) 라우팅 프로토콜로 계층적 라우팅을 지원
    - 
- BGP

### DHCP(Dynamic Host Configuration Protocol)

- 네트워크에서 장치들이 IP 주소 및 기타 필요한 네트워크 설정을 자동으로 할당받을 수 있도로 도와주는 프로토콜
- 초고속 인터넷 서비스 회사에서 개인의 서버 설치를 막기 위해 IP 주소를 자동으로 할당

### DNS(도메인 네임 시스템)

- 도메인 네임과 IP 주소를 테이블 형태로 모아놓은 서버
- 루트 DNS → 국가 DNS → 지역 DNS

## 4. 전송 계층

> 데이터 전송의 정확성과 순서를 보장하는 계층, 네트워크 계층에서 올라온 데이터를 정리하고 데이터를 프로그램에 전달
> 

### TCP

- 송신측과 수신측의 연결이 이루어진 것을 확인한 다음에 데이터를 전송하는 프로토콜
- 전송할 데이터가 큰 경우 데이터를 적절한 크기로 분할하여 각각의 부분에 TCP 헤더를 추가
- 신뢰성이 요구되는 데이터 전송 시 사용
- 전송시 데이터는 TCP 계층을 거쳐 IP 계틍으로 전달됨

### TCP 헤더

- 헤더의 크기: 20바이트
- 제어비트
    - SYN 비트: 연결을 요청하는 것(1)
    - ACK 비트: ‘응’이라는 응답을 하는 것 (1)

### UDP

- 송신측과 수신측의 연결 고정을 거치지 않고 일방적으로 데이터를 보내고 수신측으로부터 따로 응답을 받지 않는 프로토콜
- 신뢰성이 요구되지 않고, 간단한 데이터를 빠른 속도로 전송할 때 사용
- 데이터의 신뢰성이 보장되진 않지만, 처리 속도가 매우 빠름

### 포트(Port)

> 응용 프로그램 구분을 위해 전송 계층이 사용하는 주소
> 
- 포트번호: 수신측에 여러 애플리케이션 중 데이터를 수신할 애플리케이션을 식별하는 번호
- Well-Known Ports: HTTP - 80, HTTPS - 443, SSH - 22

## 5. 응용계층

> 사용자가 직접 상호작용하는 네트워크의 최상위 계층, 사용자 앞 요청을 처리
> 

- HTTP
    - 웹에서 데이터를 전송하기 위한 비보안 프로토콜
- HTTPS
    - 보안을 추가한 프로토콜
    - SSL 또는 TLS 암호화를 사용하여 데이터 전송을 보호

### 보안소켓층(SSL, Secure Sockets Layer)

- 웹클라인트와 서버 사이의 보안 연결
- 정보를 암호화
- 공중키 암호화(PKE)

### TLS(Transport Layer Security)

- 인터넷을 통한 통신을 안전하게 보호하는 프로토콜
- 인터넷 상의 데이터 전송을 암호화
- SSL의 후속 프로토콜로, SSL의 보안 취약점을 개선하고 더 강력한 암호화와 보안을 제공

### FTP(File Transfer Protocol)

- 인터넷을 통해 컴퓨터 간 파일 송수신을 위한 프로토콜
- FTP 서버에게 파일을 업로드하거나 반대로 다운로드할 수 있음

### SSH(Secure Shell)

- 네트워크 상에서 컴퓨터나 서버와 안전하게 통신하고 원격으로 관리할 수 있는 프로토콜
- 암호화된 연결을 사용하여 주로 보안이 중요한 환경에서 원격 로그인, 파일 전송, 원격 명령어 실행을 위해 사용

### 전자우편(E-mail)

- SMTP
- POP3
- IMAP
