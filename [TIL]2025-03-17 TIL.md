## 구조체 배열

- C언어에서는 배열을 값(call by value)으로 전달할 수 없음

### 1. 구조체 배열을 포인터(struct *)로 전달

- 가장 일반적인 방법으로, 배열의 첫 번째 요소를 가리키는 포인터로 전달

✅**장점:**

배열의 크기가 고정되지 않으므로 유연하게 사용 가능.

❌**단점:**

배열의 크기 정보를 따로 전달해야 함.

### 2. 배열 크기를 명시하여 전달 (`struct arr[N]`)

✅**장점:**

배열처럼 직관적인 코드 작성 가능.

❌**단점:**

함수가 특정 크기의 배열만 받도록 제한됨.

## 공용체(union)

> 공용체(union)는 구조체와 비슷하지만, 여러 멤버가 **하나의 메모리 공간을 공유**하는 특징을 가진다. 즉, 모든 멤버가 같은 메모리 주소를 공유하며, 한 번에 하나의 값만 저장 가능하다.
> 

### 특징

- 모든 멤버가 같은 메모리 공간을 공유 → 한 번에 하나의 데이터만 저장 가능
- 메모리 크기는 가장 큰 멤버의 크기로 결정됨
- 마지막으로 저장된 값만 유효

### 장점

- 메모리 절약 가능
- 다형성 → 다양한 타입 저장 가능
    
    ```c
    #include <stdio.h>
    
    union Value {
        int i;
        float f;
        char str[20];
    };
    
    // 데이터 유형 구분
    struct Variable {
        int type;  // 1 = int, 2 = float, 3 = string
        union Value data;
    };
    
    int main() {
        struct Variable var;
    
        var.type = 1;
        var.data.i = 100;
        printf("정수 값: %d\n", var.data.i);
    
        var.type = 2;
        var.data.f = 99.9;
        printf("실수 값: %.2f\n", var.data.f);
    
        return 0;
    }
    ```
    
- 네트워크 패킷 데이터 처리
    
    ```c
    #include <stdio.h>
    
    union Packet {
        unsigned char rawData[4];
        struct {
            unsigned char header;
            unsigned char command;
            unsigned char data;
            unsigned char checksum;
        } fields;
    };
    
    int main() {
        union Packet packet;
        packet.rawData[0] = 0xAA;
        packet.rawData[1] = 0xBB;
        packet.rawData[2] = 0xCC;
        packet.rawData[3] = 0xDD;
    
        printf("헤더: 0x%X\n", packet.fields.header);
        printf("명령어: 0x%X\n", packet.fields.command);
    
        return 0;
    }
    ```
    

## 열거형(enum)

- 상수 값들의 집합을 의미 있는 이름으로 정의하는 데 사용된다.
- 내부적으로 int로 저장되므로 보통 4바이트(`sizeof(int)`) 크기를 가짐
- 기본적으로 0부터 1씩 증가
- 특정 값을 직접 할당 가능
- `switch` 문에서 의미있는 코드 작성 가능

### 가장 많이 쓰이는 용도

- 상태 값(`enum STATUS { IDLE, RUNNING, ERROR }`;)
- 옵션 값(`enum Color { RED, GREEN, BLUE };`)
- 구조체와 조합하여 의미있는 데이터 관리

## 날짜 처리 관련(time.h)

### **1. `time_t` - 현재 시간 가져오기**

`time_t`는 C에서 **시간을 저장하는 기본 자료형**입니다.

```c
#include <stdio.h>
#include <time.h>

int main() {
    time_t now;
    time(&now);  // 현재 시간을 가져옴

    printf("현재 시간 (초 단위): %ld\n", now); //현재 시간 (초 단위): 1710272536
    return 0;
}
```

📌 **설명**

- `time_t now;` → 현재 시간을 저장할 변수 선언.
- `time(&now);` → 현재 시간을 `now`에 저장.
- **초 단위(`Epoch Time`, 1970-01-01 00:00:00 UTC부터 경과한 초 수)**.

---

### **2. `ctime()` - 초 단위 시간을 문자열로 변환**

```c
#include <stdio.h>
#include <time.h>

int main() {
    time_t now;
    time(&now);

    printf("현재 시간 (문자열 변환): %s", ctime(&now)); 
    // 현재 시간 (문자열 변환): Tue Mar 12 14:35:36 2024
    return 0;
}
```

📌 **설명**

- `ctime(&now);` → `time_t` 값을 사람이 읽을 수 있는 문자열로 변환.

❌ **단점**

- **지역화(Localization) 불가능**.
- **스레드 안전하지 않음** (`static` 버퍼 사용).

---

### **3. `localtime()` - `struct tm`으로 변환**

```c
#include <stdio.h>
#include <time.h>

int main() {
    time_t now;
    struct tm *localTime;

    time(&now);
    localTime = localtime(&now);  // ✅ 현재 시간을 `struct tm` 구조체로 변환

    printf("현재 시각: %d년 %d월 %d일 %d:%d:%d\n",
           localTime->tm_year + 1900,  // ✅ `tm_year`는 1900년 기준
           localTime->tm_mon + 1,      // ✅ `tm_mon`은 0부터 시작
           localTime->tm_mday,
           localTime->tm_hour,
           localTime->tm_min,
           localTime->tm_sec);
		// 현재 시각: 2024년 3월 12일 14:35:36
    return 0;
}
```

📌 **설명**

- `localtime()`을 사용하면 `struct tm`으로 변환하여 개별 항목을 접근 가능.
- `tm_year + 1900` → **년도는 1900부터 시작하므로 보정 필요**.
- `tm_mon + 1` → **월은 0부터 시작하므로 +1 필요**.

---

### **4. `strftime()` - 형식화된 날짜/시간 출력**

```c
#include <stdio.h>
#include <time.h>

int main() {
    time_t now;
    struct tm *localTime;
    char buffer[100];

    time(&now);
    localTime = localtime(&now);

    strftime(buffer, sizeof(buffer), "현재 시간: %Y-%m-%d %H:%M:%S", localTime);
    printf("%s\n", buffer);
    // 현재 시간: 2024-03-12 14:35:36

    return 0;
}
```

📌 **설명**

- `strftime()`을 사용하면 원하는 형식으로 날짜/시간을 출력 가능.
- **형식 지정자**
    - `%Y` → 4자리 연도 (`2024`)
    - `%m` → 2자리 월 (`03`)
    - `%d` → 2자리 일 (`12`)
    - `%H` → 24시간제 시 (`14`)
    - `%M` → 분 (`35`)
    - `%S` → 초 (`36`)

---

### **5. `difftime()` - 두 시간 차이 계산**

```c
c
복사편집
#include <stdio.h>
#include <time.h>

int main() {
    time_t start, end;

    time(&start);
    for (volatile long i = 0; i < 1000000000; i++);  // ✅ 일부러 시간 지연
    time(&end);

    double elapsed = difftime(end, start);  // ✅ 시간 차이 계산
    printf("경과 시간: %.2f 초\n", elapsed);
    // 경과 시간: 3.52 초

    return 0;
}

```

📌 **설명**

- `difftime()`을 사용하면 두 `time_t` 값의 차이를 초 단위로 계산 가능.

---

### **6. `mktime()` - `struct tm`을 `time_t`로 변환**

```c
#include <stdio.h>
#include <time.h>

int main() {
    struct tm futureDate = {0};

    futureDate.tm_year = 2024 - 1900;  // ✅ 2024년
    futureDate.tm_mon = 6 - 1;         // ✅ 6월
    futureDate.tm_mday = 1;            // ✅ 1일

    time_t futureTime = mktime(&futureDate);
    printf("변환된 초 값: %ld\n", futureTime);
    // 변환된 초 값: 1719859200

    return 0;
}
```

📌 **설명**

- `mktime()`을 사용하면 `struct tm`을 `time_t` 값으로 변환 가능.
- 날짜 계산할 때 유용.

---

📌 **가장 많이 사용하는 함수**

- `time()` → **현재 시간 얻기**
- `localtime()` → **구조체로 변환하여 개별 정보 추출**
- `strftime()` → **사용자 지정 형식으로 출력**
- `difftime()` → **시간 차이 계산**
- `mktime()` → **날짜 계산 및 변환**
