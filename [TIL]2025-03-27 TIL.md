# [TIL] - 2025.03.25 프로젝트 오류 해결 과정 
### 1. `select()` 종료 시 오류 (10038)

- **문제**: `main()`에서 소켓을 `closesocket()` 한 뒤에도 `receiver_thread()`가 `select()` 중이면 `WSAENOTSOCK (10038)` 오류 발생
- **해결**:
    - `shutdown_event` 사용하여 스레드에게 종료 신호 전달
    
    ```c
      select(0, &read_fds, NULL, NULL, NULL);
      int result = select(0, &read_fds, NULL, NULL, NULL);
      if (result == SOCKET_ERROR) {
          int err = WSAGetLastError();
          if (err == WSAENOTSOCK) {
              printf("[Receiver] 닫힌 소켓 감지. 종료\n");
              break;
          }
          fprintf(stderr, "[Receiver] select() 오류: %d\n", err);
          break;
      }
    ```
    

---

### 2. `worker_thread` 종료가 안 되는 문제

- **문제**: `queue_not_empty_event`가 `INFINITE`로 설정되어 있으면 `shutdown_event`를 감지 못함
- **해결**:
    - `WaitForMultipleObjects()` 사용 → `queue_event`와 `shutdown_event` 동시에 감지 가능
    

---

### 3. `WAIT_OBJECT_1` 정의 오류

- **원인**: `WAIT_OBJECT_1`은 Windows API에 직접 정의돼 있지 않음
- **해결**: `WAIT_OBJECT_0 + 1`으로 직접 비교하거나 매크로 정의 사용

```c
#define WAIT_QUEUE_EVENT (WAIT_OBJECT_0 + 1)
```

---

### 4. `OrderStatus` → 문자열 변환

```c

const char* order_status_to_string(OrderStatus status) {
    switch (status) {
        case PENDING: return "미체결";
        case MATCHED: return "체결";
        case CANCELED: return "취소";
        default: return "알 수 없음";
    }
}
```

---

---

### 5. 특정 계좌 주문 조회 시 malloc 처리

- **추천 방법**: 먼저 `SELECT COUNT(*)`로 주문 개수 조회
- 그 개수만큼 `malloc()` → `get_orders_by_account()` 호출

```c
c
복사편집
int count = get_order_count_by_account(account_id);
StockOrder* order_arr = malloc(sizeof(StockOrder) * count);

```

---
