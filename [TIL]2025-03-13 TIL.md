# 포인터 실습

## 함수 포인터

- 함수 포인터 배열을 만들어 동적으로 함수 바인딩 가능
- 문자열 포인터 `str++` 시 포인터 위치 이동 주의

```c
#include <stdio.h>
#include <ctype.h>
#include <stdlib.h>
#define SIZE 3
#define ROW 3
#define COL 3
#define B_SIZE 50

void my_strlen(char* str) {
	int cnt = 0;
	while (*str++) cnt++;
	printf("\n결과: %d", cnt);

}

void uppercase(char* str) {
	char* origin = str;
	while (*str) {
		*str = toupper(*str);
		str++;
	}
	printf("\n결과: %s", origin);

}

void lowercase(char* str) {
	char* origin = str;

	while (*str) {
		*str = tolower(*str);
		str++;
	}
	printf("\n결과: %s", origin);

}

void func_ptr2() {
	int choice;
	void (*fp) (char(*)) = NULL;
	char str[B_SIZE];

	printf("\n1. 문자열 길이 구하기");
	printf("\n2. 대문자로 변환하기");
	printf("\n3. 소문자로 변환하기");

	printf("\n선택: ");
	scanf_s("%d", &choice);
	getchar();

	printf("\n변환된 문자열 입력: ");
	gets_s(str, B_SIZE);

	switch (choice)
	{
		case 1:
			fp = my_strlen;
			break;
		case 2:
			fp = uppercase;
			break;
		case 3:
			fp = lowercase;
			break;
		default:
			break;
	}

	fp(str);

}
```

### void 타입의 함수 포인터

- 아래 코드처럼 쓰면 좋은 점은?
    
    ⇒ **반환형을 다른 함수나 매개변수가 다른 함수를 함수포인터로 처리**할 수 있다.
    
- 함수 포인터 배열의 반환형을 casting 연산하여 처리하는 방법입니다.
- 함수 호출할 때 반환형을 casting 연산 후 호출

```c
// 함수 포인터 배열 선언 (반환값 void로 설정)
void *operations[4] = { sum, max, min, square  };

// 반환 타입을 void로 casting 
(void (*)(int **, int, int))operations[choice]) (ptr_array, ROWS, COLS);    

// 반환 타입을 int로 casting
((int (*)(int**, int, int))operations[choice]) (ptr_array, ROWS, COLS);  
```

## 이중 포인터

- 이중 포인터를 넘길 때 형 변환 주의

```c
void arr_sum(int **pp) {
	int sum = 0;
	for (int i = 0; i < ROW; i++) for (int j = 0; j < COL; j++) sum += pp[i][j];
	printf("\n배열의 합은 %d", sum);
}

void arr_max(int **pp) {
	int max = -100000;
	for (int i = 0; i < ROW; i++) for (int j = 0; j < COL; j++) max = pp[i][j] > max ? pp[i][j] : max;
	printf("\n배열의 최대값은 %d", max);
}

void arr_min(int **pp) {
	int min = 100000;
	for (int i = 0; i < ROW; i++) for (int j = 0; j < COL; j++) min = pp[i][j] > min ? pp[i][j] : min;
	printf("\n배열의 최소값은 %d", min);
}

void arr_square(int* pp[]) {
	for (int i = 0; i < ROW; i++) for (int j = 0; j < COL; j++) pp[i][j] *= pp[i][j];
	printf("\n배열의 제곱값은\n");
	for (int i = 0; i < ROW; i++)
	{
		for (int j = 0; j < COL; j++) printf("%4d", pp[i][j]);
		printf("\n");
	}

}

void ptr_ex2() {
	void (*operations[4]) (int [ROW][COL]) = {arr_sum, arr_max, arr_min, arr_square};
	int choice, array[ROW][COL] = {
		{0, 1, 2},
		{3, 4, 5},
		{6, 7, 8}
	};

	int* ptr_arr[ROW];
	for (int i = 0; i < ROW; i++) ptr_arr[i] = array + i;

	printf("\n연산 방법을 선택하기");
	printf("\n(0: 합, 1 : 최대값, 2 : 최소값, 3 : 제곱): ");
	scanf_s("%d", &choice);
	operations[choice]((int **)ptr_arr);

}

```
