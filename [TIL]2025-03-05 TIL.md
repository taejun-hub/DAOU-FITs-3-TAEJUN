## 0. 오리엔테이션

### 수업 목표

- 메모리 절약
- 클린 코드

### 수업 목표

1. 컴퓨터 구조론
    1. https://github.com/okskna/ComputerArchitecture_Summary
2. 운영체제론
3. C언어 메모리 관리 S/W
4. 클린코드 작성 방법
5. 리눅스 명령어

### 폰노이만 아키텍처(Stored Program Concept)

- 프로그램이란 것은 메모리에 저장되는 컨셉으로 컴퓨터 구조가 디자인되어야 한다.

### 레지스터

- CPU 내부에  존재하는 초고속 임시 저장 장치
- 레지스터 종류
    - **프로그램 카운터(PC)** : 다음에 인출할 명령어의 주소를 보유하고 있는 레지스터
    - **누산기(AC)** - Data의 연산결과를 일시적으로 저장하는 레지스터
    - **명령어 레지스터(IR)** - 현재 실행중인 명령어를 저장하는 레지스터
    - **기억장치 주소 레지스터(MAR)** - PC에 저장된 명령어 주소가 system 주소 버스로 출력되기 전에 일시적으로 저장

## 1. CPU의 내부구조

- 메인보드 안에 CPU, 메모리, 시스템 버스가 있음
- CPU 내부에는 ALU, 제어장치와 여러 레지스터가 있음
- 모니터, 키보드, 마우스 등은 메인보드 내 시스템 버스와 연결되어 있고 , 입출력 장치라 부른다

## 2. 운영체제

- 운영체제는 컴퓨터의 하드웨어와 소프트웨어를 관리하고, 사용자가 컴퓨터를 쉽게 사용할 수 있도록 지원하는 시스템 소프트웨어
- 응용 프로그램이 사용하는 CPU시간, 기억공간, 입출력 등 하드웨어 자원을 할당하고 배분

### 운영체제의 주요 구성

- 커널(Kernel)
    - 운영 체젝의 핵심 부분
    - 하드웨어와 소프트웨어 사이에 통신을 담당
    - 시스템의 모든 기본적인 작업을 관리(메모리, 프로세스, 파일 시스템, 장치 드라이버)
    
- 쉘(Shell)
    - 사용자와 커널 사이의 인터페이스 역할을 하는 프로그램
    - 명령어 해석기로서 작동
    
- 

## 3. C언어 개요

- 식별자가 `_대문자` → 내장 함수(변수, 상수)
- `#` ⇒ 전처리기
- `#pragma once`
    - 헤더 파일의 중복 포함을 방지하는 지시문
    - 한 번 포함된 헤더 파일이 동일한 소스 파일에서 다시 포함되지 않도록 막아준다.
    - 이는 C/C++에서 헤더 파일이 여러 번 포함되는 것을 방지하기 위한 전통적인 방식인 **Include Guard**(`#ifndef`, `#define`, `#endif`)의 대체제
    

### DLL 파일

> **DLL(Dynamic Link Library, 동적 링크 라이브러리)** 파일은 **공유 라이브러리**로, 여러 프로그램에서 공통으로 사용할 수 있도록 **코드와 데이터(함수, 클래스, 리소스 등)를 포함한 파일**
> 
- **동적 링크(Dynamic Linking) 사용** → 실행 중 필요한 기능만 로드하여 메모리 절약
- **공유 라이브러리 역할** → 여러 프로그램이 동일한 DLL을 공유하여 코드 중복 방지
- **업데이트 가능** → 프로그램을 수정하지 않고도 DLL만 교체하면 기능 업데이트 가능
- **의존성 문제 발생 가능**→ 특정 DLL이 없거나 버전이 다르면 프로그램 실행 오류 발생 (DLL Hell 문제)

## 3. C언어 자료형

### 자료형 종류

- 정수형 - char(1byte), short(2byte), int(4byte), long(4byte)
- 실수형 - float(4byte), double(8byte), long double(8byte)
- 문자형 - 유니코드, 아스키코드

### 자료형 표현 방법

- 양의 정수/음의 정수
    - 부호 비트 + 숫자비트(가수부)
- 실수
    - 고정수숫점
    - **부동소수점**(표준)
