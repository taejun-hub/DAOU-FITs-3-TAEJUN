<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="like">
    <select id="selectOne" parameterType="Like" resultType="Like">
        <![CDATA[
            select member_id, board_id from likes
            where member_id = #{member_id} and board_id = #{board_id}
        ]]>
    </select>
    <insert id="insert" parameterType="Like">
         <![CDATA[
        insert into likes
            ( member_id, board_id )
        VALUES
            ( #{member_id}, #{board_id} )
        ]]>
    </insert>
    <delete id="delete" parameterType="Like">
        <![CDATA[
        delete likes
        where member_id = #{member_id} and board_id = #{board_id}
        ]]>
    </delete>


</mapper>